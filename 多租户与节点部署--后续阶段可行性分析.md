# 后续阶段（Phase 3–7）可行性分析

> 更新日期：2026-08-29
> 分析基线：`codex/phase2-multi-tenant-runner @ b1ae13d`（已推送个人 fork，工作区干净）
> 用途：在编写 Phase 3 源码级规格之前，对 Phase 3–7 的整体可行性与关键风险做一次结论性分析，作为下一步决策依据。本文是**决策与风险分析**，不是施工计划；具体阶段规格开工前另行编写。

## 一、总体结论（先看这段）

1. **剩余范围总体可行**，没有技术死路。全部关键依赖（Redis Streams + go-redis、Telegram/企业微信 SDK、OpenTelemetry、Compose）都已在前序阶段做过 Spike 或可直接验证，不存在"未知能否实现"的黑盒。
2. **真正的风险是工作量与验证成本，不是技术**。Phase 3–7 里最耗时的是：Redis Streams 可靠性语义的闭环验证、企业微信真实协议适配与 Mock 契约、OTel 端到端 Trace、审计/SQL 仓储，以及最后的设计文档与故障演示。这三块预计占剩余工作量 60% 以上。
3. **按阶段顺序推进是正确选择，但 Phase 5 的 IM 部分存在外部依赖阻塞**（企业微信真实凭据/回调），应在规格中把"真实联调"与"本地可验收的 Mock 契约闭环"明确解耦，避免被外部环境卡住整条主线。
4. **Phase 6 需要引入 SQL 平台仓储**，这是目前唯一会改变生产依赖图的新增项（PostgreSQL）。建议按"仓储接口 + 内存/文件实现先行、PostgreSQL 后接"的方式推进，让 Phase 6 的审计/配置不阻塞前序演示。

## 二、逐阶段可行性分析

### Phase 3：Gateway/Worker 消息可靠性

**结论：可行，难度中等，规格必须先行。**

- 技术可行性高：Redis Streams + Consumer Group 在 go-redis v9 中有完整 API（`XAdd`、`XReadGroup`、`XAck`、`XPending`、`XAUTOCLAIM`），与已锁定的 go-redis v9.11.0 兼容；单进程内把 `Runtime` 拆成 Gateway/Worker 两个启动角色属于既有架构的自然切分。
- 真正的难点不是 API，而是**语义闭环**：
  1. "回复已写入回复流但任务未 ACK"与"任务已 ACK 但回复发送失败"两类补偿语义必须先定死。
  2. 可重试错误与不可重试错误的分类，及其到 HTTP/日志/指标的映射。
  3. Inbox 状态机的原子转换（`processing / succeeded / failed`）、最大尝试次数、退避上限、终态保存位置。
  4. Pending 列表在长期失败下的增长与 DLE（死信）边界。
- 必须先在规格里锁定的细节（交接摘要十-A 已列）：Consumer Group 命名、消费者命名、租约续期/重新认领 API、ACK 顺序、Demo 出站发送器与 Mock 模型测试替身。
- **风险提示**：Redis Streams 不保证同一 Session 的严格全局顺序（多个消费者并发时按 partition/时间戳）。Phase 3 是单 Worker 验证，顺序问题可暂缓；但规格里要写清"Phase 4 用 Session 锁串行化"是补齐顺序的既定手段，不能把 Phase 3 的 Streams 设计写成强顺序承诺。

### Phase 4：双 Worker 与共享状态

**结论：可行，难度中等偏高，是可靠性阶段的技术核心。**

- 双 Worker 各自按 `tenant + app + version` 创建等价 Runner 的方案已在 Phase 0/1 确认；Session/Memory 走共享 Redis 后端，跨 Worker 可见性已在 Phase 1.5 真实验证，这部分没有新风险。
- 新增的复杂度集中在**同一 Session 的并发串行化**：
  1. 需要 Session 级锁或版本号（Redis lock / Lua 原子操作），保证重复投递或双 Worker 同时领取同一 Session 的任务不会并发写坏 Session。
  2. 锁的租约、续期、过期后接管，必须与 Inbox 租约复用同一套时序，避免"任务被重新认领但 Session 锁未释放"。
  3. Worker 故障接管验证：停一个 Worker，Pending 任务被另一个重新认领——这条路径必须真实跑通并留证据。
- 已有事实可复用：Phase 1.5 记录的上游 Memory 非原子容量检查、Event 相同时间戳无序等已知限制，在 Phase 4 设计中必须显式规避（Event 需要单调时间戳或显式序列号）。
- **风险提示**：Session 锁与 Inbox 租约的交互最容易出现死锁/竞态，规格里应给出明确的状态机图和超时矩阵，测试矩阵至少要覆盖：重复消息不二次执行、同一 Session 并发只串行、Worker 故障后接管、Redis 短暂中断恢复。

### Phase 5：Telegram、企业微信与 Web UI

**结论：可行，但企业微信部分是外部依赖阻塞最集中的阶段，需拆分验收口径。**

- Telegram：`go-telegram/bot v1.23.0` 已在 Spike 验证（长轮询/Webhook、自定义 HTTP、429/5xx、消息映射），本地可用 Mock/长轮询闭环验收，无阻塞。
- 企业微信：`silenceper/wechat/v2 v2.1.14` 已确认覆盖发送、AES 与签名算法，但**内部自建应用普通回调仍需平台薄适配**（验签 + AES-CBC 解密 + 消息解析）。这是已确认的缺口，不是未知项。
  - 关键约束：真实回调联调依赖负责人提供的环境/凭据，当前不确定。因此规格必须把"真实协议适配逻辑（可追溯 Mock 契约测试）"与"真实账号联调（外部依赖）"分开，本地交付口径为前者。
  - Mock 必须可追溯到官方协议或固定测试向量，不能用两份随意 JSON 代替适配器实现——这是负责人明确的要求。
- Web UI：按负责人意见做"类似 DeepSeek 网页版"的聊天界面 + 预置绑定选择。**页面不能直接提交可信 `tenant_id`**，服务端必须通过 `ChannelBinding` 解析租户。实现简单，但这条安全约束必须写进规格并加测试。
- **风险提示**：
  1. 企业微信回调验签/解密的真实凭据未就绪，联调可能拖到很晚，甚至依赖负责人协助。规格应定义"若外部环境不可得，本地验收到 Mock 契约 + Web UI 演示为止，并明确记录外部阻塞"。
  2. 消息长度、频率限制、异步回复、图片/文件、撤回与失败重试是 IM 平台限制，规格需逐一给出处理策略（即使首版只支持文本，也要写明边界）。
  3. 群聊的 `session_id` 生成规则（群主体作 runner scope）已在 Phase 0 确认，Phase 5 需落地并测试跨群/跨租户隔离。

### Phase 6：治理、安全、审计与观测

**结论：可行，范围必须收敛到"最小闭环"，并引入 SQL 平台仓储。**

- 工具白名单、IM 用户权限、敏感信息脱敏：逻辑简单，但脱敏要贯穿日志、Trace、错误响应和审计所有路径，需建立统一脱敏层并加测试。当前 Memory 工具已全部关闭，工具面是从零定义，反而简化了白名单起点。
- 危险操作二次确认：按交接摘要，是否纳入代码最小闭环需结合范围确认；若实现，使用共享 Redis + TTL + 原子消费（有现成 Lua 原子化经验可复用）。建议默认"文档 + 可选增强"，不阻塞主线。
- OTel 端到端 Trace：需先核验 tRPC-Agent-Go 的 OpenTelemetry 能力与本次链路（IM callback -> Gateway -> Worker -> Runner -> Tool -> Session/Memory -> 回复）的传播方式。**这是 Phase 6 唯一需要先做技术核验的点**，建议在规格阶段安排一次只读源码核验或小 Spike，避免实现时才发现框架不支持自动埋点。
- 审计日志：需要存储位置。**建议引入 PostgreSQL 平台仓储（Repository 接口先行）**——这是剩余阶段唯一会新增进生产依赖图的决策点。审计字段已在题目中固定（tenant/channel/user/session/agent/tool/decision/latency/error_type/cost/trace_id）。
- **风险提示**：
  1. SQL 仓储若直接进生产依赖，会带回 Phase 1.5 已知的 PostgreSQL Summary 时区问题影响面（审计表不受影响，但要避免复用官方 SQL Session 子模块，审计表自建即可）。
  2. 脱敏与审计若在项目末期才做，容易变成大范围返工；建议把脱敏层作为基础设施在 Phase 5 前置搭好。

### Phase 7：部署、故障验证与最终交付

**结论：可行，但这是最容易"看起来简单、实际最耗时"的阶段。**

- Compose 本地闭环（Gateway + 2 Worker + Redis + PostgreSQL + 前端）技术上无难点。
- 真正风险：
  1. **Windows shell 验证缺口**：当前机器无 WSL / Git Bash，`start.sh` 启停闭环未真实执行。Compose 验收必须用 Docker Desktop 的 Linux 容器环境完成，并在文档中说明可迁移性，不能只保证 Windows 手工运行。
  2. 故障演示矩阵（重复消息、Worker 故障、后端短暂不可用、模型超时、回复失败）每条都要可复现并有证据，耗时占比高。
  3. 交付文档量大：2000–4000 字设计文档 + 架构图 + 时序图 + 数据模型 + 多后端说明 + 不少于 8 个风险。建议在 Phase 5/6 实现过程中**随写随存**，不要留到最后集中补。
  4. 最终 PR 需按负责人规范提供个人 fork 分支 `feature/{your_name}` 链接；当前仍未配置服务仓库 `upstream`，交付前需补齐。

## 三、关键决策点（需要用户/负责人确认）

| # | 决策点 | 现状 | 建议 |
| --- | --- | --- | --- |
| 1 | 危险操作二次确认是否进代码最小闭环 | 交接摘要标注"需结合范围确认" | 默认文档 + 可选增强，不阻塞 Phase 3–5 |
| 2 | PostgreSQL 平台仓储是否本阶段进生产依赖 | 目前 SQL 子模块未进生产依赖 | 建议 Phase 6 以自建 Repository 接口 + 自建审计表接入，不复用官方 SQL Session 子模块 |
| 3 | 企业微信真实联调口径 | 负责人可协助，时机未定 | 本地验收到 Mock 契约 + Web UI 演示；真实联调作为外部依赖单独跟踪 |
| 4 | OTel 是否框架原生支持 | 未核验 | 规格阶段先做一次只读源码核验或小 Spike |
| 5 | Web UI 是否复用现有 `trpcservice/web` 占位包 | 占位包 | 按"聊天界面 + 预置绑定选择"最小实现，服务端解析可信租户 |
| 6 | `start.sh`/Compose 验收环境 | 本机无 WSL/Git Bash | 用 Docker Desktop Linux 容器完成闭环，文档写明可迁移性 |
| 7 | 服务仓库 `upstream` | 未配置 | Phase 7 交付前配置并同步原仓库 |

## 四、整体风险清单（Phase 3–7）

1. **工作量集中**：可靠性语义验证、故障演示、IM 真实协议适配、OTel、审计、交付文档合计占剩余工作量约 60%+，规格必须先收敛边界再动手。
2. **企业微信外部依赖**：真实凭据/回调未就绪，可能拖到项目末期；必须用 Mock 契约测试守住本地可验收底线。
3. **Session 并发串行化**：Phase 4 的 Session 锁与 Inbox 租约交互是最容易出现死锁/竞态的地方，需要状态机图与超时矩阵。
4. **Redis Streams 顺序语义**：不承诺强顺序，Phase 3 设计不得写成强顺序承诺，Phase 4 用锁补齐。
5. **OTel 传播链路**：框架是否支持自动埋点未核验，规格阶段需先验证。
6. **SQL 仓储引入**：新增生产依赖的决策点，需避免带回官方 SQL 子模块已知缺陷。
7. **Windows 验证缺口**：shell 启停与 Compose 闭环依赖 Linux 容器环境，需在 Docker 中完成并留证据。
8. **脱敏/审计晚做返工**：建议脱敏层在 Phase 5 前置搭好，审计随写随存。
9. **文档交付量大**：设计文档、架构图、时序图、数据模型、多后端说明、风险清单要在实现过程中随写随存。

## 五、下一步建议（Phase 3 开工路径）

1. 从 `b1ae13d` 创建个人 fork 分支，按负责人规范命名 `feature/{your_name}`（现有 `codex/phase2-multi-tenant-runner` 不改名）。
2. 先编写并审阅 **Phase 3 源码级规格**：固定 Redis Streams（`agent.tasks` / `agent.replies`）、Consumer Group 命名、Inbox 唯一键（`tenant_id + channel_binding_id + platform_message_id`）、Inbox 状态机与原子转换、租约/重试/退避上限、两类补偿语义、可重试/不可重试错误分类、Demo 出站发送器与 Mock 模型测试替身。
3. 规格审阅通过前不修改生产代码；保持 Phase 2 的 `/healthz`、`/readyz`、Demo Binding 可信路由和租户隔离行为不变。
4. Phase 3 验收通过（重复投递、租约恢复、失败重试、ACK 顺序、跨租户 Inbox 唯一键、服务重启恢复）后再进入 Phase 4 设计。
