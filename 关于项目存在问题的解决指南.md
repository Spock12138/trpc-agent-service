# 关于项目存在问题的解决指南

> 更新日期：2026-08-24
> 当前仓库：`D:\project\OpensourceTencent\trpc-agent-service`
> 用途：记录实施 `多租户与节点部署--逻辑的plan.md` 之前的关键问题与已确认的解决方向。本文件是**问题与决策记录**，不是施工计划；具体阶段规格在开工前另行编写。
> 实现分工：代码由 ChatGPT 5.6 sol 按规格实现，用户负责审阅与验收，架构决策由用户把关（可协助）。

---

## 一、总体可行性结论（先记住这几点）

1. **方案可行、依赖真实**。tRPC-Agent-Go 已发布到 v1.11.2（118 个版本、60 位贡献者，腾讯业务在用），plan 依赖的 `runner.Runner`、`session/redis`、`memory/redis`、OpenTelemetry、`NewRunnerWithAgentFactory`（按配置动态建 Runner）全部真实存在。
2. **最大风险是工作量，不是技术**。代码可以靠 AI 快速产出，但**验证、调试、故障演示、文档**才是真正吃时间的部分，至少预留 1/3 时间。
3. **验收人是框架贡献者**（`liuzengh` 在 tRPC-Agent-Go 贡献者名单中），实现不能是拼凑 demo，核心的租户隔离、Runner 缓存、幂等必须写对。

---

## 二、问题与解决（逐条记录）

### 问题 1：四周时间能否做完全部范围？

- **决策**：接受全量范围，交给 ChatGPT 5.6 sol 实现，用户负责审阅，认为可以完成。
- **保留提醒**：
  - 验证、故障演示、题目要求的 2000-4000 字设计文档 + 架构图 + 时序图 + 数据模型 + 风险清单，不缩水、也省不掉时间。
  - 阶段 0 和阶段 1 先小步试水一次：只让 sol 写"最小 Agent 闭环"，用户审一次、跑一次，摸清产出质量后再放心放大。这不是推翻"每阶段一次性写"的决定，只是先验证流程。

### 问题 2：LLM API 用哪家？

- **决策**：使用 **DeepSeek API**，key 已申请。
- **说明**：tRPC-Agent-Go 官方示例原生支持：
  ```go
  openai.New("deepseek-chat", openai.WithVariant(openai.VariantDeepSeek))
  ```
  无需额外适配。
- **注意**：key 和 base_url 必须走环境变量/配置文件，**不得硬编码、不得出现在日志、Trace 或错误报告中**（阶段 6 脱敏验收会查）。

### 问题 3：企业微信接入类型？

- **决策**：暂不锁定"自建应用回调"等具体类型，**等负责人提供具体情况**；当前实现先选一个合适候选。
- **IM 组合采用方案 A**：**Telegram + 企业微信**。
  - 企业微信按官方协议 + Mock 契约测试先实现（签名校验、AES-CBC 解密、文本解析、回复格式）。
  - 真实联调后置，等负责人提供配置后填写验证；负责人在聊天中明确表示"这个验证我可以到时候代码的时候填写"。
  - 目标是拿到真实配置后**只补凭据和回调配置，不重写**租户解析、消息处理或 Agent 主链路。

### 问题 4：本地环境能跑 Docker 吗？

- **决策**：可以本地安装，Windows 上使用 **Docker Desktop**。
- **分工**：Redis（任务队列、Session/Memory、Inbox、租约）与 PostgreSQL（配置、绑定、审计）都用 Docker 容器跑。

### 问题 5：和 sol 的分工边界？

- **决策**：规格、验收、架构决策由用户（或我协助）把关；**sol 只负责按规格写代码**。
- **节奏**：每个阶段让 sol 一次性写；但按问题 1 的提醒，阶段 0/1 先小步验证一次产出质量。
- **关键**：每个阶段开工前必须有**详细规格**（接口签名、数据结构、失败语义、验收标准），不能只丢给 sol 一句"按 plan 实现"。

### 问题 6：Git 策略时机？

- **决策**：在**阶段 0** 就完成：
  - 添加并核验 `upstream` → `https://github.com/liuzengh/trpc-agent-service.git`
  - 创建功能分支，不把代码堆在 `main`
  - 规划 PR 拆分（建议 5 组可审查提交，见逻辑 plan 第 6 节）
- **注意**：建分支前先处理工作区未跟踪文件（`多租户与节点部署--交接摘要.md`、`开源实战题目.md`、`多租户与节点部署--逻辑的plan.md`、`关于项目存在问题的解决指南.md`），**禁止在未检查时使用 `git add -A`**。

### 问题 7：Web UI 与两种 IM 的边界（负责人反馈澄清）

- 负责人原话："至少两种 IM 需要在代码中实现""可以做一个 UI 的 IM 自行验证""类似 DeepSeek 网页版，输入问题查看模型回复"。
- **正确理解**：Web UI 是**第三个演示/验证入口**，**不能替代**两种 IM 的代码实现。
- Web UI 只做聊天主界面 + 预置绑定选择；页面选择租户/Agent 时**不能直接信任浏览器提交的 `tenant_id`**，必须由服务端通过 `ChannelBinding` 解析可信租户。

---

## 三、技术要点与易踩的坑

1. **模块路径陷阱**：GitHub 仓库地址是 `github.com/trpc-group/trpc-agent-go`，但 **Go 模块路径是 `trpc.group/trpc-go/trpc-agent-go`**（README 中所有 import 均为此路径）。`go.mod` 必须锁定该路径 + 具体版本。
2. **Go 版本**：官方要求 **Go 1.21+**，当前 `go.mod` 的 `go 1.21` 兼容，无需升级；锁定版本后不跟随浮动 `main`。
3. **版本锁定**：使用开发当日的最新稳定版（当前为 v1.11.2），在 `go.mod` 中固定，保证可复现构建。
4. **Runner 方案有框架原生支撑**：`runner.NewRunnerWithAgentFactory` 支持按请求/配置动态创建 Agent，可作为"按 `tenant_id + agent_app_id + config_version` 缓存 Runner"的实现依据。
5. **Session/Memory 共享**：使用 `session/redis`、`memory/redis` 官方后端保证跨 Worker 可见；同一 Session 的并发串行需要用 Redis 锁/版本号由平台层自行实现。
6. **可靠性口径**：采用"至少一次投递 + 幂等处理"，不宣称分布式绝对只执行一次；回复统一由 Gateway 出站，Worker 不直接持有 IM 凭据。
7. **安全**：外部消息中的 `tenant_id` 不可信，租户必须由已验签的渠道账号通过 `ChannelBinding` 映射得到；未知绑定/冲突/后端不可用一律 fail closed。

---

## 四、下一步待办（阶段 0 门槛）

1. 核验 `git status`、远端与 `upstream` 最新状态，保护未跟踪文件后配置 `upstream`、建功能分支。
2. 确认并锁定 `trpc.group/trpc-go/trpc-agent-go` 版本，验证官方 `examples/runner` 本地能跑通（DeepSeek key 配置）。
3. 确定 Telegram SDK 选型，固定企业微信协议范围（自建应用回调作为首选候选，标注暂定）。
4. 输出阶段 0 详细施工规格（架构图、消息时序图、数据模型、缺口清单、验收矩阵），作为给 sol 的第一份施工指令。

---

## 五、负责人沟通要点存档（陈雷@腾讯）

- 至少两种 IM 需在代码中实现，Web UI 不能替代。
- 可以抽象统一的 IM 接口，分别适配企业微信、微信、飞书等渠道。
- 各 IM 已有较成熟 SDK，优先复用 GitHub 上的现成 SDK，不必从头实现全部协议。
- 本地可做 UI 版 IM（类似 DeepSeek 网页版）自行验证消息流程；后续运行代码时再填真实配置。
- 如果企业微信验证有困难，负责人后续可以统一协助验证。
