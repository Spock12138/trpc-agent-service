# 多租户与节点部署--交接摘要

> 更新日期：2026-08-28
> 当前实施仓库：`D:\project\OpensourceTencent\trpc-agent-service`
> 当前阶段：Phase 2 已在 `codex/phase2-multi-tenant-runner` 完成实现、本地验收并通过审阅，已拆分为 3 个 commit 并推送到个人 fork 的 `origin/codex/phase2-multi-tenant-runner`。只读 JSON Catalog、可信 Binding、InMemory/Redis BackendProvider、RunnerRegistry、两租户 Session/Memory 隔离、存储冷切换、真实 Redis 7 和真实 DeepSeek 双租户 Smoke 均已完成。本轮准备进入 Phase 3 源码级规格，见“九-D、Phase 2 关闭结论与遗留问题”和“十-A、Phase 3 开工入口”。
> 用途：作为下一轮对话窗口的事实入口，记录已经确认的决策、仍需验证的问题、旧材料的使用边界和下一步顺序。开始新一轮工作时应先完整阅读本文件，再核验仓库实时状态；不要把本文记录的 Git 状态当成永久事实。

## 一、项目已经切换，旧计划不能直接执行

本课题当前应基于以下独立服务仓库开发：

```text
D:\project\OpensourceTencent\trpc-agent-service
```

远端关系的目标是：

```text
origin   -> https://github.com/Spock12138/trpc-agent-service.git
upstream -> https://github.com/liuzengh/trpc-agent-service.git
```

截至本次只读核验，只有 `origin`，尚未配置 `upstream`。后续准备正式开发时需要添加 `upstream`，但本轮没有执行该操作。

旧工作区仍位于：

```text
D:\project\OpensourceTencent\trpc-agent-go-multi-tenant-workspace
```

其中的《多租户与节点部署的逻辑plan--施工版.md》和《多租户与节点部署--交接摘要.md》仍有参考价值，可继续用于：

- 理解题目要求和完整消息链路。
- 参考多租户隔离、可信渠道绑定、Inbox 幂等、共享存储和无状态 Worker 的风险分析。
- 参考企业微信官方协议核对、真实适配器加 Mock 契约测试的原则。
- 参考 Redis 二次确认、审计、脱敏、OpenTelemetry、多后端迁移和故障恢复的设计问题。
- 参考“逻辑计划 -> 阶段规格 -> 实现与验证 -> 回写交接摘要”的工作方式。

但旧计划**只能作为需求研究材料，不能再直接作为实施依据**。原因是旧计划面向修改 `trpc-agent-go` 框架源码，假设 OpenClaw、Runtime Profile、Telegram Gateway 等能力已经位于当前代码库中，并设计了单 Runner 路由 Wrapper、框架公共 API 审查和一组框架上游 PR。现在的 `trpc-agent-service` 是独立应用仓库，应该把 tRPC-Agent-Go 当作依赖，在平台层实现租户、IM、调度、部署和治理能力。OpenClaw 是可独立运行的成品示例，不再作为本服务直接复用 API 或运行链路的依赖候选。

因此，下列旧结论必须在新仓库中重新验证或重新设计：

- 不能直接照搬“单 Runner + Session/Memory 路由 Wrapper”；已确认采用“每个 Worker 按 Agent App 创建并缓存 Runner”。阶段 1 已完成 singleflight、排空、Close 和 race 验证，容量与 TTL 仍需以后按真实规模标定。
- 不能直接照搬面向 `trpc-group/trpc-agent-go` 的 PR 拆分和公共 API 归属。
- 不能把旧仓库 `AGENTS.md` 的“CI 必须稳定、可复现”当作新仓库约束；新仓库目前没有 `AGENTS.md`。
- 不直接 import 或包装 OpenClaw 的公开 API；OpenClaw 的 Telegram 分层、插件注册、消息转换、重试等只作为设计参考，是否采用类似插件体系仍以当前项目实际需要为准。
- 不能以修改框架内部能力为默认方案；优先在 `trpc-agent-service` 平台层完成真实调用链路。

## 二、新仓库的目标与当前形态

`trpc-agent-service` 的 README 将本题定义为“基于 tRPC-Agent-Go 的多租户节点化 Agent 部署平台”。题目以架构设计为主，不要求实现完整生产系统，但交付物明确包含一份 GitHub 实现代码。因此最终不能只有设计文档、空接口或 Mock，至少需要一条真实可运行、可演示的核心链路。

README 要求覆盖：

- 多租户模型、节点部署、无状态 Worker 和租户隔离。
- Session、Memory、Summary、Artifact、Knowledge、Audit 等多后端方案。
- 至少两种 IM，其中至少包含微信或企业微信。
- IM 验签、账号绑定、用户映射、消息去重、回复和失败重试。
- 治理、审计、脱敏、指标和 OpenTelemetry Trace。
- 故障恢复、容量估算、最小部署和生产部署建议。
- 架构图、核心时序图、数据模型、幂等策略、多后端说明和不少于 8 个风险。

阶段 0 记录中的“仓库只是骨架”已被 Phase 1 和 Phase 1.5 实现更新。当前仓库已经具备配置、身份派生、RunnerCache、单进程 executor、官方 Redis Session/Memory Service、平台 `RedisBackend` 生命周期包装、CLI、HTTP 接口和自动化测试；真实 IM、双 Worker、队列、SQL 平台仓储、Web UI 和完整可观测性仍未实现。README 中给出的目录仍只是示范，后续结构继续服从模块职责。

## 三、负责人反馈及其含义

2026-08-20 收到负责人的邮件回复，核心原话为：

> Go 的实战会基于这个仓库来做 https://github.com/liuzengh/trpc-agent-service，不要求 CI 流水线，而是真实的项目。

后续用户补充转述了负责人关于 IM 的意见：

- 如果需要，负责人可以提供企业微信环境。
- 允许使用 Mock 模拟数据。
- 最后展示需要设置前端，可以模拟发送数据、模拟回复。

2026-08-21 用户进一步向负责人确认“Web UI 是否可以完全替代两种 IM 实现”，负责人明确回复：

- 至少两种 IM 需要在代码中实现，Web UI 不能代替这两种 IM 的代码实现。
- 可以抽象统一的 IM 接口，再分别适配企业微信、微信、飞书等渠道。
- 各 IM 已有较成熟的 SDK，可优先复用 GitHub 上的现成 SDK，不必从头实现全部协议细节。
- 当前没有真实配置时，可以在本地实现一个 UI 版 IM，自行验证消息流程；后续运行代码时再填写真实 IM 配置。
- 如果企业微信验证存在困难，负责人后续可以协助验证。
- Web UI 可以做成类似常见大模型网页聊天界面的形式：输入问题并查看模型回复。

目前可以可靠得出的结论是：

1. `trpc-agent-service` 是实际交付仓库，重点是能运行和展示的项目，不是专门为 CI 编写的测试改造。
2. Mock 是允许的，适合用于稳定复现验签、解密、异常、重试和重复投递等场景。
3. “允许 Mock”不等于“可以只有 Mock”。README 要求 GitHub 实现代码，负责人又强调真实项目，因此仍应实现真实的企业微信协议适配逻辑。
4. 两种 IM 当前按“成熟 SDK/真实协议适配 + Mock 数据验证”推进。按照负责人的思路，优先调查和使用 GitHub 上已有的成熟 IM SDK，不从头重复实现成熟的底层收发能力；Mock 请求应符合真实协议并尽量来源于官方示例、SDK 输出或可追溯的固定测试向量，不能用两份随意编写的 JSON 代替适配器实现。
5. 已确认采用 `Telegram + 企业微信`，两者都转换为平台统一的入站/出站消息格式；不直接复用 OpenClaw API。
6. 企业微信首版接入类型已确定为内部自建应用；`silenceper/wechat/v2` 已完成初步协议匹配 Spike，真实回调适配和账号联调留到 IM 阶段验证。
7. Web UI 是两种 IM 之外的本地验证和演示入口，不是完整管理后台；范围为聊天主界面和预置绑定/Agent 选择，节点、审计、Trace 通过调试接口或日志查看。
8. Web UI 选择租户或 Agent 时也不能绕过可信绑定。页面应选择预置的模拟渠道账号或绑定，服务端再通过 `ChannelBinding` 解析 `tenant_id + agent_app_id`，不能直接信任浏览器提交的 `tenant_id`。
9. 阶段 1 的真实 Agent 采用可配置的 OpenAI-compatible 模型入口；自动化测试使用 Mock，真实演示所需的 endpoint、模型名和凭据尚未提供。

因此当前推荐的 IM 交付口径是：**两种真实适配器代码 + 可追溯的 Mock 契约测试 + 一个轻量 Web UI + 后续按配置接入真实平台**。拿到真实账号或 Bot 后，理想状态应当只是补充凭据和回调配置，不重写租户解析、消息处理或 Agent 执行主链路。

## 四、已经确认的基础技术选择

### 1. tRPC-Agent-Go 版本

- GitHub 仓库地址是 `github.com/trpc-group/trpc-agent-go`，Go module path 是 `trpc.group/trpc-go/trpc-agent-go`；Go 引用必须使用后者。
- 2026-08-24 实际执行 `go list -m -versions` 和 `go list -m -json`，最新稳定标签为 `v1.11.2`，发布时间为 `2026-08-20T04:03:23Z`，模块声明 Go 1.21；GitHub 远端 tag 也已交叉核对。
- 阶段 0 推荐开始开发时锁定 `trpc.group/trpc-go/trpc-agent-go v1.11.2`；若正式引入当日出现新版本，先检查 release notes 和 API 变化再决定，不自动漂移。
- 在 `go.mod` 中锁定具体版本号，保证其他人可以复现构建。
- 不长期跟随浮动的 `main` 分支。
- OpenClaw 不作为直接依赖，其 Go 版本和模块版本不再约束本项目；项目最低 Go 版本由实际采用的 tRPC-Agent-Go 核心版本和 IM SDK 等真实依赖共同决定。

### 2. 平台与 OpenClaw 的边界

已经确认“参考设计思想，但不直接复用 OpenClaw API”：

- `trpc-agent-service` 定义自己的统一入站/出站消息契约和 Channel Adapter 接口。
- 可信租户解析、ChannelBinding、幂等、权限和 Gateway/Worker 交接属于平台层。
- OpenClaw 下的 Telegram 是其成品应用内部实现，不把它当作独立 SDK，也不直接 import 或包装其公开 API。
- 可以参考 OpenClaw 的 `API Client -> Channel Adapter -> Plugin/Registry` 分层、消息标准化、会话、附件、重试和测试思路，但插件体系是否照此实现、接口如何定义，必须服从当前项目的真实需求。
- Telegram、企业微信等 IM 优先采用 GitHub 上维护成熟、许可证合适且覆盖所需协议的 SDK，再由服务项目编写薄适配层接入统一契约。已确认 Telegram 首选 `github.com/go-telegram/bot v1.23.0`、备选 `telegram-bot-api/v5 v5.5.1`；企业微信首版按内部自建应用验证，并对 `silenceper/wechat/v2 v2.1.14` 做协议匹配 Spike。

### 3. Gateway 与 Worker

- 放在同一个代码仓库，做成两个启动角色，例如 `trpc-service gateway` 和 `trpc-service worker`。
- Gateway 负责接收渠道消息、验签、识别租户、幂等认领和投递任务。
- Worker 负责选择 Agent App Runner、执行模型与工具、读写 Session/Memory 并产生回复。
- Channel Adapter 跟随 Gateway；Worker 不依赖本机唯一状态。
- 本地最小运行可先启动一个 Gateway 和一个 Worker，多节点验收明确按同一台机器启动一个 Gateway 和两个 Worker 进程或容器执行，不要求个人学生准备两台物理机器。
- 多节点演示至少应证明：两个 Worker 都能处理任务；同一 Session 可以跨 Worker 继续；停止一个 Worker 后另一个能够接管；重复消息不会执行两次；Worker 本地内存不保存唯一会话状态。

### 4. Redis 与 SQL

Redis 用于短期、快速、需要原子竞争的状态，例如：

- 任务分发或队列。
- Inbox 消息去重、处理租约和超时恢复。
- 同一 Session 的并发协调。
- 危险操作二次确认、TTL 和原子消费。
- 临时状态和热点缓存。

SQL 用于长期、结构化、需要查询和审计的数据，例如：

- Tenant、Agent App 和配置版本。
- ChannelBinding 与用户映射。
- 审计日志和配置发布记录。
- 长期业务元数据。
- 选择 SQL 后端的租户所使用的持久化 Session/Memory。

简化理解：Redis 记录“现在正在发生什么”，SQL 记录“系统长期需要保留什么”。具体数据库产品和 Schema 尚未确定。

当前实施优先级进一步明确为：

- Redis 是最终多节点闭环的最低保证，必须实际接入；不能以 InMemory 作为最终多节点存储。
- SQL 是优先实现项，用于长期配置、绑定和审计；但不应在早期阻塞真实 Agent、Redis 和双 Worker 主链路。
- 如果工期不足，SQL 至少要有明确的数据模型、Repository/Storage Adapter 边界和落地方案；是否完整接入由新施工计划结合四周期限确定。
- README 要求的向量库、对象存储、外部 Memory 等多后端能力，首版可以主要通过统一抽象、选型比较和同步/迁移设计说明覆盖，不要求同时把所有后端写成生产级实现。

Phase 1.5 已将 Redis 固定为官方 `session/redis`、`memory/redis` Service，并由平台 `RedisBackend` 负责延迟初始化、`Ready`、命名空间、工具策略和幂等关闭；SQL 子模块只完成兼容性结论，不进入生产依赖。

### 4-A. tRPC-Agent-Go 多模块适配器事实纠正

2026-08-25 重新按 Go module 边界核对后，确认此前“tRPC-Agent-Go `v1.11.2` 没有 Redis Session/Memory 实现”的结论不准确。准确事实是：

- `trpc.group/trpc-go/trpc-agent-go v1.11.2` 根模块的下载目录中不包含 `session/redis`、`memory/redis` 和 SQL 适配器源码。
- tRPC-Agent-Go 是多模块仓库；Redis、SQL、S3、Qdrant、Milvus 等适配器以独立子模块发布，不会因为只依赖根模块而自动进入服务依赖图。
- Go Module Proxy 查询和 `go mod download -json` 已确认远程存在 `session/redis v1.11.0`、`memory/redis v1.11.0`、`storage/redis v1.11.0`、`session/mysql v1.11.2`、`session/postgres v1.11.0` 等正式版本。
- `storage/redis`、`storage/mysql` 等主要提供可复用客户端或连接实例管理，不等于一个能够保存所有平台数据的统一业务存储；Session、Memory、Knowledge、Artifact 仍分别使用自己的服务接口和适配模块。
- “远程已经发布”不等于“自动与当前服务兼容”；本阶段已在隔离消费者、真实 Redis/PostgreSQL 和 Go 1.21.13 下完成组合验证，并把结果写入 `docs/stage1.5-storage-spike.md`。

Phase 1.5 的唯一结论为 **B：官方实现 + 平台薄包装**。生产代码使用 `trpcservice/storage/redis_backend.go`；旧 `redis.go`/`ErrUnsupported` 快照适配器已删除，不保留 fallback、双读或运行时开关。包装层不重写 CRUD，只协调独立 Session、Memory、健康检查 client 的延迟初始化、`<REDIS_KEY_PREFIX>:official-v1` 命名空间、`/readyz` 恢复和固定关闭顺序。

### 5. Runner 生命周期

- 旧方案可通俗概括为：“所有请求共用一个 Runner，运行过程中由路由 Wrapper 根据服务端内部路由信息选择对应的数据后端。”
- 已确认方案可通俗概括为：“执行前先确定 Agent App，再选择或创建该 Agent App 对应的 Runner；每个 Worker 保存自己的 Runner 缓存。”
- 对旧方案已经确认的主要顾虑是路由 Wrapper 需要完整覆盖 Session/Memory 的各种能力、可选接口和生命周期，设计与维护复杂；此前把问题概括为“所有地方都必须携带 AppName，容易漏传或错传”并不严谨，不应作为否定旧方案的既定依据。
- 方案使用 `tenant_id + agent_app_id + config_version` 作为缓存身份，并且不会按每条消息创建 Runner；其创建、缓存、更新、淘汰、资源占用和跨 Worker 行为由阶段 1 Spike 固定参数。
- Session 和 Memory 必须位于共享后端，不能只保存在 Runner 或某个 Worker 的内存中。
- 未知租户、未知 Agent App、配置冲突或后端不可用时应 fail closed，不得回退到其他租户或默认 App。

### 6. Git 与最终 PR

- 当前 `origin` 指向个人 fork；如需向上游贡献，先提供个人 fork 分支链接，再按维护者要求向 `liuzengh/trpc-agent-service` 发起 PR。
- `origin` 保持指向个人 fork。
- 正式开发前添加 `upstream` 指向原始仓库。
- 功能在独立分支开发，不直接堆在 `main`。
- 仓库模块路径应继续使用 `github.com/liuzengh/trpc-agent-service`，不要因为在个人 fork 开发就改成 `github.com/Spock12138/...`。
- 是否拆成多个 PR 尚需结合上游偏好和阶段边界确认；至少要避免一个难以审查的巨型 PR。

#### 负责人确认的个人 Fork 提交规范（2026-08-28）

- 后续提交直接在自己的 fork 建立分支，分支名称固定为 `feature/{your_name}`；`{your_name}` 替换为提交者可识别的名称。
- 交付时提供个人 fork 中该分支的链接，便于负责人按分支识别提交人；这条规则优先于旧材料中未明确命名的临时分支写法。
- 已完成的 Phase 2 分支 `codex/phase2-multi-tenant-runner` 保留历史，不改名；从 Phase 3 开始的新开发分支按上述规范创建。
- 分支内仍按阶段和职责拆分 commit，禁止把学习笔记、交接摘要和独立上游事项使用 `git add -A` 一并提交。

## 五、Go 版本与 OpenClaw：当前结论

### 已核实的事实

旧 `trpc-agent-go-replay-v2` 是多模块仓库，不是全仓统一使用一个 Go 版本：

```text
根 go.mod       -> go 1.21
test/go.mod     -> go 1.24.4
openclaw/go.mod -> go 1.24.1
```

用户之前的回放一致性测试位于独立 `test` 模块，所以记忆中的 Go 1.24 来源之一是 `test/go.mod`。OpenClaw 本身又是独立模块，也要求 Go 1.24.1。当前本机实际 `go version` 为 Go 1.26.4，但本机版本不等于项目必须声明的最低版本。

当前 `trpc-agent-service/go.mod` 保持 `go 1.21`；Phase 1.5 已在 Go 1.21.13 下通过生产 test/build 和 Redis/MySQL/PostgreSQL 隔离消费者 test/build，不受 OpenClaw 的 Go 1.24.1 要求约束。

### 如何决定

- 不再安排“能否直接 import OpenClaw”的依赖 Spike，也不为复用其 Telegram 成品实现而引入 OpenClaw module。
- OpenClaw 源码仍可作为只读研究材料，用于理解成熟实现中的分层、插件注册、长轮询、附件、会话和重试设计。
- 阶段 0 应改为核对 tRPC-Agent-Go 核心版本，并调研候选 IM SDK 的维护状态、许可证、Go 版本、协议覆盖、测试便利性和与统一 Adapter 的适配成本。
- 不因为本机安装 Go 1.26.4 就把项目无必要地提升到 Go 1.26.4；最终使用实际依赖要求的最低合理版本并在 `go.mod` 中锁定。

## 六、建议的新项目主链路

新项目的目标闭环暂定为：

```text
演示前端 / Telegram / 企业微信
  -> 平台 Channel Adapter
  -> 协议验证与统一消息契约
  -> ChannelBinding 可信租户与 Agent App 解析
  -> Inbox 幂等认领
  -> Gateway 投递任务
  -> Worker 按最终确认的 Runner 生命周期方案选择执行环境
  -> tRPC-Agent-Go Agent / Runner / Tool
  -> 共享 Session / Memory 后端
  -> 平台统一出站消息
  -> IM Adapter / 演示前端回复
  -> Trace / Metric / Audit
```

外部消息中的 `tenant_id` 不能作为可信身份。租户应由已经通过协议验证的渠道账号映射得到：

```text
channel + external_account_id / bot_id / corp_id
  -> ChannelBinding
  -> tenant_id + agent_app_id + credential_ref
```

未知绑定、冲突配置和后端不可用都应拒绝处理，不回退默认租户。

## 七、实施阶段与下一步计划

阶段 0、1 和 1.5 已关闭并保存到远端。Phase 2 已于 2026-08-27 在 `codex/phase2-multi-tenant-runner` 完成本地实现与验收，并于 2026-08-28 通过审阅，随后已拆分为 3 个 commit 推送到个人 fork；完整结论见 `docs/stage2-multi-tenant.md`。

### 阶段 0：已完成

- 已完成仓库基线、tRPC-Agent-Go API 核验、IM SDK 候选比较、Runner 方案比较、平台契约和范围验收。
- 已确认五项阶段 0 决策，详见 `docs/stage0-acceptance.md`。

### 阶段 1：最小真实 Agent 服务

- 先复核 tRPC-Agent-Go `v1.11.2` release notes、module path 和 Go 1.21 构建要求。
- 在独立临时模块验证 Telegram/企业微信候选 SDK，以及 RunnerCache 的 singleflight、版本排空、Close 和 race 行为。
- 再修改 `go.mod` 锁定实际依赖，生成阶段 1 详细接口、错误语义和测试矩阵。
- 配置并创建一个真实 tRPC-Agent-Go Agent/Runner。
- 提供最小 HTTP 请求入口、健康检查和真实 Agent 回复。
- 先用共享 Redis Session/Memory 或阶段规格明确的最小可替换后端证明服务可启动和请求可完成。

### 阶段 1：已完成与审阅结论

- 阶段 1 已通过外部审阅，结论是“完成，可进入阶段 2”。这里的完成是带边界的完成：真实 Agent 最小闭环、真实 Redis 验证和验收矩阵已完成，不等于完整生产平台已完成。
- 已通过 `go test ./...`、`go test -race ./...`、`go build ./...`、`go vet ./...`，并在 Go 1.21.13 下再次通过全量测试。
- 已真实验证 `trpc-redis` 容器：`redis:7-alpine`、端口 `6379`、`redis-cli ping` 返回 `PONG`；真实服务的 `/readyz`、Session/Memory 写入和跨 runtime 读取均连接 Docker Redis。
- 已真实验证 HTTP -> executor -> Runner -> DeepSeek -> Docker Redis 链路；演示数据已清理，Redis 容器保持运行。
- `miniredis` 仍保留在自动化单元测试中。它是纯内存 Redis 服务端模拟，不落盘；`go-redis` 对它仍使用 Redis 协议，但它不能替代 Docker Redis 的网络、进程和真实运行特性。
- `REDIS_URL` 的服务配置接受 `localhost:6379` 简写并规范化；当前 `REDIS_SMOKE_URL` 测试入口要求完整 `redis://localhost:6379/0`，这是一个已知的小一致性问题，建议阶段 2 统一。
- Docker 权限策略已确认：默认保持 `workspace-write`；需要 Docker 时只对明确的单条 `docker ...` 命令临时提权，仅访问 Docker，不读取或修改其他系统、工作区外文件或密钥。

### 阶段 1.5：官方 Redis/SQL 子模块兼容性 Spike（已完成）

阶段 1.5 已关闭，目标是消除模块边界误判、验证官方后端契约并在同一阶段完成生产 Redis 接入。完整命令、模块图和证据见 `docs/stage1.5-storage-spike.md`。

- 隔离消费者位于被忽略的 `.stage15-spike/redis`、`.stage15-spike/mysql`、`.stage15-spike/postgres` 和 `.stage15-cache`；使用远程发布物、`GOWORK=off`，无消费者 `replace` 或 `go.work`。
- 生产 `go.mod` 直接声明 `session/redis` 与 `memory/redis`；`storage/redis v1.11.0` 由官方 Service 传递引入并以 `// indirect` 固定解析版本，三者均已在隔离消费者中验证。
- Redis 根模块 `v1.11.2` 与 `session/redis`、`memory/redis`、`storage/redis v1.11.0` 通过编译期接口断言、Runner 构造、miniredis 契约和独立 Docker Redis 7 契约；覆盖 CRUD、State、Event 顺序/过滤、Summary、TTL、前缀隔离、并发写、恢复、取消和 Close 生命周期。
- 生产 Session 固定为 HashIdx-only、同步持久化、无 TTL、启用用户 Session 索引，不配置 Summarizer 或异步持久化；测试中的固定 Summarizer 已证明 Summary 可跨实例读取。
- 官方 Memory 的非原子容量检查已记录；生产设置无限容量并关闭 Add/Update/Search/Load/Delete/Clear 六种 Memory 工具、Extractor 和强制配额。
- `RedisBackend` 会释放所有已返回的部分构造 Service，并在构造前后检查取消；Runtime 关闭通过生命周期闸门等待活跃请求排空。官方 Memory 内部固定 `context.Background()` Ping 仍无法被外部 context 中断，保留为上游限制。
- tRPC-Agent-Go Runner 可能记录 Session 持久化错误后继续输出模型回复；平台对显式返回的存储错误会健康探测并映射 `503`，被上游吞掉的中途持久化失败留给后续 Inbox/幂等阶段处理。
- 生产采用唯一 **B：官方 Redis Service + 平台生命周期薄包装**。`RedisBackend` 延迟初始化，不在 `Runtime.New` 连接网络；Session、Memory、健康检查使用独立 client，Redis 恢复后 `/readyz` 无需重启即可恢复，关闭顺序固定为 RunnerCache -> Session -> Memory -> health client。旧快照适配器、`ErrUnsupported`、fallback、双读和迁移均已移除。
- MySQL 完成发布模块、接口、选项、schema、事务、初始化和关闭路径的 sqlmock/静态核验；PostgreSQL 16 完成真实建表、索引、Session/Memory CRUD、跨连接、并发和关闭后重建。SQL 子模块不进入生产 `go.mod`。
- PostgreSQL `session/postgres v1.11.0` 的 Summary 时间戳/时区边界缺陷已用专用表和真实容器复现并记录；MySQL Session 发布源码的 `%w` 日志格式触发默认 vet，`-vet=off` 契约通过。这两项是上游限制，不改变 Redis 方案结论。
- 当前工具链完成生产 test/race/build/vet；Go 1.21.13 完成生产 test/build 及 Redis/MySQL/PostgreSQL 隔离消费者 test/build，并保持 `/healthz`、`/readyz`、Demo HTTP 响应与错误映射不变。

#### README 验收标准 7：框架复用与平台新增核对矩阵（实际结果）

| 平台能力 | 可复用框架包/独立子模块 | 当前证据与状态 | 平台必须新增 |
| --- | --- | --- | --- |
| Agent 与执行 | 根模块 `agent/*`、`runner`，当前锁定 `v1.11.2` | Phase 1 已真实运行并通过测试 | AgentApp 注册、配置发布、租户路由、RunnerRegistry |
| InMemory Session/Memory | 根模块 `session/inmemory`、`memory/inmemory` | 根模块源码与 Go 包可见；适合测试/单机 | 部署模式能力检查，禁止多 Worker 把 InMemory 当唯一共享状态 |
| Redis Session/Memory | `session/redis v1.11.0`、`memory/redis v1.11.0`、`storage/redis v1.11.0` | 隔离消费者、miniredis、真实 Redis 7、24 路并发和生产 Runtime 均通过；生产已接入 B 路径 `RedisBackend` | 租户级 `StorageProfile`、凭据引用、指标和后续多 Worker 路由 |
| SQL Session/Memory | `session/mysql v1.11.2`、`session/postgres v1.11.0`、`memory/mysql v1.11.0`、`memory/postgres v1.11.0`，另有 SQLite/MongoDB 子模块 | MySQL 静态/sqlmock 通过（上游 Session vet 限制）；PostgreSQL 16 真实最小契约通过并记录 Summary 时区缺陷；不进生产依赖 | 租户级选择、schema/表前缀策略、凭据引用、迁移和回滚 |
| Summary | `session.Service` 摘要能力及各 Session 子模块实现 | 官方 Redis Session Summary 可跨实例读取；PostgreSQL `v1.11.0` freshness/时区边界限制已记录，SQL 暂不生产接入 | 摘要策略配置、租户配额、失败与重建策略 |
| Knowledge | 根模块 `knowledge`、`knowledge/vectorstore/inmemory`；独立 `qdrant v1.11.0`、`milvus v1.11.0` | 根模块及远程子模块已确认，未接入服务 | KnowledgeProfile、租户索引/collection 隔离、同步迁移 |
| Artifact | 根模块 `artifact/inmemory`、`artifact/cos`；独立 `artifact/s3 v1.11.0`、`storage/s3 v1.11.0` | 根模块及远程子模块已确认，未接入服务 | ArtifactProfile、对象 key 命名、租户权限、生命周期和审计 |
| IM Channel | 框架/OpenClaw 仅作分层和消息处理参考；Telegram/企业微信使用已选成熟 SDK | 已完成 SDK/协议 Spike，真实 Adapter 未实现 | 自有 ChannelAdapter、验签解密、ChannelBinding、用户映射和回复重试 |
| 多租户与后端路由 | 无可直接复用的完整平台实现 | README 明确要求平台层完成 | Tenant、AgentApp、ConfigVersion、StorageProfile、BackendProvider/Factory、可信绑定与 fail closed |
| 可靠任务处理 | Redis 客户端可复用，Agent Runner 可复用 | Inbox/Outbox、Streams、租约尚未实现 | Gateway/Worker、TaskQueue、幂等、租约、重试、故障接管 |
| 治理与观测 | Plugin/Guardrail/Callbacks、OpenTelemetry 基础能力 | 框架能力存在，租户级链路尚未接入 | 工具白名单、权限、预算/审批、脱敏、租户指标、Audit Log、端到端 Trace |
| 管理与长期元数据 | 数据库客户端或 SQL 基础模块可复用 | 平台表模型尚未实现 | Admin API、Tenant/Binding/配置/发布/Audit Repository |

证据来源包括：当前服务 `go.mod` 与生产回归；`.stage15-spike/` 隔离消费者的 module graph、接口/契约测试和真实 Redis/PostgreSQL 结果；Go Module Proxy 的 `go list -m -versions`、`go mod download -json` 结果；以及本地只读源码树 `trpc-agent-go-multi-tenant-workspace` 中各子模块的实现核对。汇总结论固定在 `docs/stage1.5-storage-spike.md`；后续任何“框架可复用”声明都必须指向可复现证据，不能仅凭 README 名称推断。

### 阶段 2：平台契约、多租户和 Runner 注册表（已完成并通过审阅）

- 已实现统一内部消息契约、Tenant/AgentApp/ChannelBinding/ConfigVersion/StorageProfile 和只读 `PresetRepository`。
- `PLATFORM_CONFIG_FILE` 支持最大 1 MiB 的严格 JSON；未设置时兼容旧环境变量。JSON 只允许 `env:<NAME>` 凭据引用，启用 AgentApp 的所有版本凭据在启动时校验。
- Demo 请求继续只提交 `binding_id`；租户、Agent、活动版本和 StorageProfile 全由服务端 Repository 派生，伪造租户字段仍在 HTTP 解码层拒绝。
- BackendProvider 按租户/Profile 选择官方 InMemory 或 Phase 1.5 Redis Service；JSON Redis 前缀加入 tenant/profile 隔离，legacy 前缀保持不变。
- RunnerRegistry 按 `tenant_id + agent_app_id + config_version` 缓存 Runner，保留 singleflight/Lease/TTL/Drain/Close，并补空闲 LRU。
- 自动化验证两个租户不串配置、Runner、Session 和 Memory；严格 readiness、Redis 故障无 fallback、恢复、配置版本切换、InMemory -> Redis 冷切换和 HTTP 兼容均通过。
- 独立真实 Redis 7 验证租户化前缀、跨 Runtime Session 和 `CLIENT PAUSE` 故障恢复；专用容器已清理。
- 当前工具链 test/race/build/vet 与 Go 1.21.13 test/build 均通过；2026-08-28 真实 `deepseek-v4-flash` 双租户目录 Smoke 也已通过。
- 真实 IM、Streams、Inbox/Outbox、双 Worker、SQL Repository、Web UI、热加载和存储迁移仍不在 Phase 2。

### 阶段 3：Gateway/Worker 消息可靠性

- 将同一仓库程序拆成 Gateway 和 Worker 两个启动角色。
- 实现任务投递、Inbox 去重、租约、失败重试和超时恢复。
- 明确回复发送与处理状态之间的补偿语义。
- 本阶段固定可靠消息契约和单 Worker 恢复语义；双 Worker 故障接管验收留到阶段 4。

### 阶段 4：双 Worker 与共享状态

- 通过 Redis 锁或版本号保证同一 Session 串行处理。
- 验证两个 Worker 都能领取任务，停止一个 Worker 后另一个能重新认领未完成任务。
- 验证同一 Session 跨 Worker 连续可见，重复消息不会重复触发 Agent。
- 明确模型超时、工具失败、Redis 暂时不可用和回复失败的处理结果。

### 阶段 5：Telegram、企业微信与 Web UI

- 按已确认范围实现 Telegram 和企业微信内部自建应用 Adapter。
- 企业微信遵循官方协议实现真实验签、解密、消息解析和回复格式。
- Mock 数据必须可追溯到官方协议或固定测试向量。
- 建立轻量前端模拟 IM 请求和展示回复；前端不能直接提交可信 `tenant_id`。

### 阶段 6：治理、安全、审计与观测

- 实现最小工具白名单、IM 用户权限和敏感信息脱敏。
- 危险操作二次确认是否纳入代码最小闭环需结合范围确认；若实现，应使用共享 Redis、TTL 和原子消费。
- 串联 IM callback、Gateway、Worker、Runner、Tool、Session/Memory 和回复的 Trace。
- 记录租户维度指标和 Audit Log。

### 阶段 7：部署、故障验证和最终交付

- 提供最小 Compose 或等价本地启动方案。
- 演示 Gateway、两个 Worker、Redis、SQL 和前端。
- 验证重复消息、Worker 故障、后端短暂不可用、模型超时和回复失败。
- 完成 README、架构文档、时序图、数据模型、多后端说明、风险清单和演示步骤。
- 按上游要求整理提交并向 `liuzengh/trpc-agent-service` 发起 PR。

每个阶段仍应遵循：

```text
先读取最新源码
-> 制定当前阶段详细规格
-> 确认接口、失败语义、兼容性和测试矩阵
-> 再实现
-> 定向验证和更广验证
-> 回写本交接摘要
```

### 代码实现、文档设计与可选增强的范围分级

新施工计划不应把题目中所有生产级能力都列为同等优先级。每个需求应明确标记为“必须编码”“文档为主”或“可选增强”，防止四周内范围失控。

**必须编码并验证的核心：**

- 真实 tRPC-Agent-Go Agent/Runner 执行链路。
- 两种 IM 的真实 SDK/协议适配逻辑及 Mock 契约测试；已确认 Telegram + 企业微信。
- 统一入站/出站消息模型、可信 ChannelBinding、多租户与 Agent App 隔离。
- Redis 共享状态、任务投递或认领、消息去重、租约/重试所需的最小可靠性能力。
- 同机一个 Gateway + 两个 Worker 的运行和故障接管验证。
- 共享 Session/Memory，确保跨 Worker 可见且不依赖单个 Worker 内存。
- 基础工具权限、敏感信息脱敏、审计和 Trace；精确深度由阶段规格收敛。
- 轻量 Web UI、健康检查和 Compose 或等价的可复现本地部署。

**以设计文档为主的生产级能力：**

- 容量估算与压测模型。
- Redis 到 SQL 的完整在线迁移、双写、灰度切流和自动回滚。
- 向量库、对象存储和外部 Memory 的完整迁移实现。
- 生产级 Kubernetes、跨机房容灾和大规模调度。
- 完整预算计费、复杂配额治理和全面灰度发布系统。

这些内容不是省略不谈，而是要在设计文档中明确数据流、接口、风险、失败处理和未来落地步骤；首版不承诺完整生产实现。

### 实施工程备忘录（指导思想）

- 项目架构必须保持模块化，IM、统一消息、租户绑定、Gateway、Worker、Agent、存储、治理和观测之间使用清晰接口，避免所有逻辑堆进启动入口或单个包。
- “多重构”统一表述为“持续小步重构”：先完成一个可测试的小闭环，再在每个阶段内及时整理边界和消除重复，不把大规模重构积压到项目末期。
- 文件或函数明显过长、同时承担多个职责、难以单独测试时要及时拆分；不机械追求行数，但不能形成难以阅读和维护的超长文件、超长函数。
- 核心模块随实现同步编写单元测试，尤其覆盖统一消息转换、可信租户解析、跨租户拒绝、Runner 注册、Redis 幂等/租约和错误路径；不要等全部功能完成后再集中补测试。
- Mock 应服务于真实协议和错误场景的稳定复现，不能替代真实业务逻辑。
- 抽象以当前真实需求为依据，不为遥远需求提前制造复杂层次；每次重构后都要运行受影响测试，保证行为没有被无意改变。

## 八、当前 Git 与工作区状态

2026-08-28 本地只读核验结果：

```text
repository: D:\project\OpensourceTencent\trpc-agent-service
branch:     codex/phase2-multi-tenant-runner
HEAD:       b1ae13d configs: Phase 2 multi-tenant example catalog
tracking:   origin/codex/phase2-multi-tenant-runner
origin:     https://github.com/Spock12138/trpc-agent-service.git
upstream:   尚未配置
```

跟踪文件工作区干净；当前快照为：

```text
codex/phase2-multi-tenant-runner @ b1ae13d
Phase 2 已拆分为实现、验收文档/README、示例配置/.gitignore 三个 commit，并推送到个人 fork
根目录仍保留前序规划、交接、用户文档和独立上游事项等未跟踪文件
origin 已配置并跟踪当前分支，upstream 尚未配置
```

《开源实战题目.md》、阶段 0/1 文档和本交接摘要均属于当前工作区文件；其中部分是前序对话创建、部分是用户已有内容。后续不得误删、回退或在未检查时使用 `git add -A`。正式添加 `upstream`、同步原仓库、创建功能分支或提交前，必须先阅读差异并保护这些文件。

当前未配置 `upstream`。Phase 1 已提交并推送到 `origin/feature/phase1-minimal-closed-loop`，Phase 1.5 已提交为 `fb3cfa3` 并推送到 `origin/feature/phase1.5-storage-spike`。当前工作分支 `codex/phase2-multi-tenant-runner` 从 Phase 1.5 正确基线创建，已建立 `origin/codex/phase2-multi-tenant-runner` 远程跟踪；旧本地 `feature/phase2-multi-tenant-runner` 仍停在 Phase 1 提交，只保留为历史分支。Phase 2 三个 commit 已推送；根目录同时存在大量前序未跟踪资料，后续不得使用 `git add -A`。临时 `.stage1-cache/`、`.stage1-spike/`、`.stage15-spike/`、`.stage15-cache/` 和明文 Key 笔记被 `.gitignore` 忽略，不应被手工强制加入 Git。

## 九、阶段 0 历史未决问题与风险

1. “最新稳定版本”需要在正式开发当日重新查询和锁定，本文不把当前本地 tag 当作永久选择。
2. Telegram 首选 `go-telegram/bot v1.23.0` 已确认；企业微信首选 `silenceper/wechat/v2 v2.1.14` 做阶段 1 协议匹配 Spike，最终 Adapter 覆盖仍需技术验证。
3. 企业微信首版接入类型已确定为内部自建应用；负责人若提供其他产品线，需要另行核对协议，不能混用 Adapter。
4. 企业微信真实环境可由负责人协助验证，但是否为必选验收、什么时候申请、凭据如何保管仍需确认。
5. 两种 IM 组合已确认采用 Telegram + 企业微信；两种 Adapter 都必须有代码，企业微信真实账号联调后置，可追溯 Mock 用于自动化验收。
6. Runner 方案已确认：每个 Worker 按 `tenant_id + agent_app_id + config_version` 有界缓存 Runner；阶段 1 已完成 singleflight、排空、Close 和 race 验证，容量 32、TTL 30 分钟仍是未按真实规模标定的保守参数。
7. 最小真实纵向链路和 Web UI 边界已确认，按 `docs/stage0-acceptance.md` 第 3-5 节执行；生产扩展以设计文档为主。
8. Gateway/Worker 的具体通信机制尚未决定。Redis Streams、队列或其他方案需结合一致性、重试和演示复杂度选择。
9. PostgreSQL 仍是平台长期配置、绑定和审计的首选 SQL；阶段 1.5 已完成官方 SQL Session/Memory 的兼容性结论，但 SQL 子模块不进入当前生产依赖，Summary 时区限制需在后续平台设计中明确处理。
10. 三个官方库问题已分别独立提交给上游；合并状态和维护者反馈需要后续关注，但不阻塞 `trpc-agent-service` 主线 Phase 2。
11. 新仓库没有 `AGENTS.md`；正式贡献前仍需检查 README、CONTRIBUTING、上游新提交或其他仓库级约束是否发生变化。
12. 负责人尚未说明线上验收环境，当前以本地开发为主；但交付应尽量使用 Linux 容器和 Compose 保持可迁移性，不能只保证在个人 Windows 环境手工运行。
13. 代码提交次数限制规则尚未公布；当前不把它作为设计阻塞项，但在规则明确前避免无意义地频繁创建远端 PR 或验收提交。
14. Go 环境的用户级 `GOPATH=D:\go\gopath` 和 `GOMODCACHE=D:\go\gopath\pkg\mod` 已写入并验证；旧终端/IDE 需要重启，机器级旧 `GOPATH` 暂未清理。
15. Git `safe.directory` 当前依赖运行环境的命令级注入，尚未持久化到用户级配置；配置 `upstream`、分支或提交前需要设置单仓库安全目录。

## 九-A、Phase 1.5 后当前风险

1. 官方发布版 Redis Memory 的容量检查仍是 `HLen` 后写入的非原子检查；生产继续设置无限容量，不把发布版 limit 当作强配额机制。本地上游修复已用 Lua 原子组合 `HEXISTS`、`HLEN` 和 `HSET`，但在修复合并并发布前不改变生产规避策略。
1-A. 官方 Redis Event 顺序按时间戳/ZSet 排序；相同时间戳不承诺通用插入顺序，后续多 Worker 阶段需定义并发序列语义。
2. 官方 Memory 构造器在创建 client 后 Ping 失败的极窄窗口不会自行 Close；平台无法取得未返回的 client，只能依靠外层 `Ready` 与恢复重试缩小窗口。
3. PostgreSQL Summary 时区问题和 MySQL `%w` 日志格式问题均已在独立分支完成修复；发布版仍包含缺陷，SQL 仍未进入生产依赖。在对应 PR 合并并发布前，平台侧规避和依赖范围保持不变。
4. Phase 1 演示快照数据按非生产数据处理；新旧键不迁移、不双读、不允许新旧进程混写。
5. Inbox/Outbox、消息去重、Redis Streams、任务租约、双 Worker、SQL 配置仓储、Web UI 和 OpenTelemetry 后端留到后续阶段；`message_id` 当前不提供幂等。Phase 2 已完成只读 JSON Catalog 和内存 Repository，但不等于完成动态平台仓储。
6. RunnerCache 的容量 32、TTL 30 分钟等仍是 Phase 1 保守值，未基于真实租户规模标定。
7. Telegram 与企业微信只完成 SDK/协议 Spike，尚未实现真实 Adapter 或真实账号联调。
8. 三个上游修复 worktree 已配置官方 fetch 与个人 fork push；`trpc-agent-service` 自身的远端和工作树状态仍应在审阅、提交 Phase 2 或进入下一阶段前单独复核。真实 DeepSeek 是外部依赖，自动化测试只依赖本地 Mock。
9. 用户已确认 Docker 当前启动。阶段 1.5 使用的独立 `trpc-stage15-redis`/`trpc-stage15-postgres` 已在验收后清理，现有其他容器未改动；后续官方问题复现可重新使用 Docker。默认 Codex 沙箱是否能直接访问 Docker named pipe 仍需按实际命令核验，不能把“Docker 已启动”与“当前终端有权限”混为一谈。

## 九-C、已核实并独立提交上游的三个问题

以下问题均来自 Phase 1.5 的隔离消费者、真实 Redis 7、PostgreSQL 16 或官方源码复核。三个问题已在互不堆叠的独立分支完成修复，并已正式提交给上游；它们不构成当前生产 B 路径的选型阻塞。完整 issue 口径见 `docs/upstream-issues.md`。上游是否合并、CI 状态和维护者反馈仍以 GitHub 页面为准，不影响本服务主线推进。

1. **PostgreSQL Summary 时区/时间戳缺陷**：分支 `codex/fix-postgres-summary-timezone`，提交 `7adbd288 session/postgres: preserve summary freshness across time zones`。修复保留 Session JSON 中带偏移的绝对 `CreatedAt`，Summary freshness 在 Go 层优先使用 JSON `UpdatedAt`，缺失时才回退数据库列；不迁移 schema，也不恢复历史提交 `a5ef5a20` 撤销的全局 `.UTC()`。修复前真实 PostgreSQL 16 跨连接用例稳定返回 `found=false`，修复后通过；子模块 `go test ./...`、`go test -race ./...`、`go vet ./...` 均通过。现已独立提交上游，后续只跟踪 CI 和维护者反馈。
2. **MySQL Session 日志格式与 vet 缺陷**：分支 `codex/fix-mysql-log-format`，提交 `75cb280e session/mysql: fix async persistence log formatting`。两处 `log.ErrorfContext(..., "%w", ...)` 已改为日志 API 支持的 `%v`；子模块 `go test ./...` 与 `go vet ./...` 已通过。现已独立提交上游，后续只跟踪 CI 和维护者反馈。
3. **Redis Memory 容量检查非原子**：分支 `codex/fix-redis-memory-limit`，提交 `83a84371 memory/redis: enforce memory limit atomically`。`AddMemory` 在配置有限容量时通过单键 Lua 原子执行 `HEXISTS`、`HLEN` 和 `HSET`；并发新增不同 ID 不得超限，满额时已有 ID 仍可幂等覆盖，同时覆盖脚本错误和异常返回值。现已独立提交上游，后续只跟踪 CI 和维护者反馈；生产仍使用无限 Memory limit。
4. **可选的次级反馈项**：Redis Memory 构造器在 Ping 失败窗口可能遗留未返回 client；Event 使用时间戳/ZSet 时相同时间戳没有插入序保证；Runner 可能记录 Session 持久化错误后仍返回模型响应。三项需要分别判断是否属于官方契约、是否已有 issue，以及是否应与前三项分开提交。

反馈问题单应统一包含：模块与精确版本、Go/OS/数据库版本、最小复现命令或测试、实际结果、期望结果、生产影响、当前规避措施和建议修复方向。不要把平台的 `RedisBackend` 薄包装、Docker 权限差异或未接入 SQL 误报成官方缺陷。

### 三个独立修复工作区

三个分支都直接基于同一官方提交，不互相堆叠：

| 顺序 | 子模块 | worktree / 分支 | 当前提交 | 远端与 PR 状态 |
| --- | --- | --- | --- | --- |
| 1 | `session/mysql` | `trpc-agent-go-fix-mysql` / `codex/fix-mysql-log-format` | `75cb280e` | 已正式提交上游；待 CI/维护者反馈 |
| 2 | `memory/redis` | `trpc-agent-go-fix-redis-memory` / `codex/fix-redis-memory-limit` | `83a84371` | 已正式提交上游；待 CI/维护者反馈 |
| 3 | `session/postgres` | `trpc-agent-go-fix-postgres` / `codex/fix-postgres-summary-timezone` | `7adbd288` | 已正式提交上游；待 CI/维护者反馈 |

- 三个修复都以 `bcd0a5679624b85810dd6e047eb63ec8867a05f4` 为直接父提交，互不堆叠；最后核验时三个 worktree 均无未提交代码改动。
- `upstream` fetch 指向 `https://github.com/trpc-group/trpc-agent-go.git`，其 push URL 已设为 `DISABLED`；`origin` 指向个人 fork，且 `push.default=current`。首次推送使用 `git push -u origin <branch>`。
- 协调工作区 `D:\project\OpensourceTencent\trpc-agent-go-multi-tenant-workspace` 仍在 `feat/multi-tenant-node-deployment-workspace@a2e8ec22`，包含用户未提交的文档改动。不要在该工作区修复，也不要清理、暂存或覆盖这些资料。
- 当前本机为 Go `1.26.4 windows/amd64`、CGO 已启用；仓库 CI 以 Go 1.21 为基线，正式提交前还需补 Go 1.21 兼容验证。当前未安装 `goimports`、`golangci-lint`、`gh`，需要时应先按仓库贡献说明补齐或采用等价的仓库脚本。
- PostgreSQL 修复后的普通测试、race、vet 和真实 PostgreSQL 16 回归均已通过；MySQL 修复后的普通测试与 vet 已通过。Redis PR 提交前按用户操作清单重新执行普通测试、race 和 vet，并只在命令真实通过后写入 PR Testing。
- 本机已有 `redis:7-alpine` 与 `postgres:16-alpine` 镜像；PostgreSQL 修复使用的临时容器 `codex-trpc-postgres-summary-timezone` 已停止，临时真实数据库测试文件已删除。
- 三个 PR 均应面向 `trpc-group/trpc-agent-go:main`，英文标题和正文遵循 `CONTRIBUTING.md`；推荐 `type/bug`。issue 编号未知时不要保留虚构的 `Fixes #...`。

## 九-B、Phase 2 已确认决策与结果

Phase 2 的产品和接口问题已在规格审阅时关闭：

1. Binding Registry 使用 Repository 接口 + 只读 JSON/内存索引，不引入 SQL。
2. 模型范围固定为 `Tenant + AgentApp + ChannelBinding + ConfigVersion + StorageProfile`，凭据只保存引用。
3. `/api/v1/demo/messages` 完全兼容，内部转换为统一消息。
4. ConfigVersion 显式、不可变，StorageProfile 是版本的一部分；后端切换冷启动，不迁移或双读。
5. 所有启用配置凭据在启动时检查；严格 `/readyz` 检查所有启用版本引用的 Profile。
6. 两租户隔离、InMemory/Redis、伪造租户、版本切换、真实 Redis 7 和 Go 1.21.13 均已验收。

## 九-D、Phase 2 关闭结论与遗留问题

### 已完成并可作为下一轮事实基线

1. **配置与领域模型**：已实现 `Tenant + AgentApp + ChannelBinding + ConfigVersion + StorageProfile`、严格只读 JSON、`env:<NAME>` 凭据引用、旧环境变量兼容和 `PresetRepository` 全量引用校验。
2. **可信路由**：Demo HTTP 契约保持兼容；客户端不能提交可信租户、Agent、版本或 StorageProfile，服务端只通过 `channel + binding_id` 解析内部身份。Binding ID 跨渠道全局唯一，避免 HMAC 身份碰撞。
3. **存储路由**：已实现 InMemory/Redis `BackendProvider`。JSON Redis 键前缀包含 tenant/profile，legacy 模式保持旧前缀；后端故障不 fallback，消息只检查选中后端，严格 `/readyz` 检查所有启用版本引用的后端。
4. **Runner 生命周期**：已实现按 `tenant_id + agent_app_id + config_version` 工作的 `RunnerRegistry`，复用 singleflight、Lease、TTL、Drain 和幂等 Close，并补充只淘汰空闲 Runner 的 LRU。
5. **隔离与切换**：两个逻辑租户在相同外部用户/会话标识下，配置、Runner、Session、Memory 均不串；`v1 -> v2` 使用新 Runner，旧活跃请求可完成；InMemory -> Redis 使用新版本冷切换，不迁移、不双读。
6. **验证**：当前工具链 `go test ./...`、race、build、vet、`go mod verify`，Go 1.21.13 test/build，以及独立真实 Redis 7 的租户前缀、跨 Runtime Session、`CLIENT PAUSE` 故障/恢复和 legacy Smoke 均通过。2026-08-28 真实 DeepSeek InMemory/Redis 双租户 Smoke 通过；专用 Redis 容器已经停止并自动删除，其他容器未修改。
7. **文档与入口**：`docs/stage2-multi-tenant.md`、`configs/phase2.example.json`、README 和 Phase 2 Plan 已更新；`start.sh` 已修正为执行 `trpc-service serve "$@"`。

### 收口结果与明确留到下一轮的问题

1. **Phase 2 Git 基线已建立**：实现、验收文档/README、示例配置/.gitignore 已拆分为 `c0c0210`、`bd1646b`、`b1ae13d` 三个 commit，并推送到个人 fork 的 `origin/codex/phase2-multi-tenant-runner`。学习笔记和独立上游事项仍未跟踪，后续继续禁止 `git add -A`。
2. **真实模型目录 Smoke 已补验**：2026-08-28 使用宿主用户环境凭据完成 `deepseek-v4-flash` 双租户调用；`/healthz`、`/readyz`、InMemory/Redis Binding、非空响应和跨租户 Session 隔离均通过。凭据未写入 JSON、日志、文档或 Git。
3. **Shell 启停闭环未运行**：当前 Windows 的 `bash` 是未安装 WSL 的占位程序，机器也没有 Git for Windows Bash。`start.sh` 已静态核对并修正，但没有实际执行 `start.sh -> /healthz -> stop.sh`；应在 Linux、WSL、Git Bash 或 Compose 环境补验。
4. **后续阶段编号已统一**：已按逻辑施工 Plan 固定为 Phase 3 可靠消息、Phase 4 双 Worker 与共享状态、Phase 5 Telegram/企业微信与 Web UI；后续阶段规格不得再沿用旧编号。
5. **真实渠道身份尚未实现**：Demo `binding_id` 只是模拟选择器，不是生产认证。Telegram 和企业微信仍只有 SDK/协议 Spike，没有真实 Adapter、验签/解密或真实账号联调。
6. **可靠消息与多节点尚未实现**：`message_id` 不幂等；Redis Streams、Inbox/Outbox、租约、重试、Session 并发串行化、双 Worker 和故障接管都在后续阶段。
7. **动态配置仓储尚未实现**：JSON Catalog 不热加载，没有 PostgreSQL Repository、发布历史或跨部署的“同版本号改内容”自动检测。生产预置配置变化需要重启；凭据原地轮换也不会自动重建缓存 Runner。
8. **存储迁移尚未实现**：StorageProfile 切换是显式冷切换，新后端从空状态开始；没有在线迁移、双写、双读或回滚。InMemory 只允许单进程开发/测试，不能作为双 Worker 的唯一共享状态。
9. **严格 readiness 是已接受但需持续评估的取舍**：任一启用版本的 Redis Profile 故障都会使整体 `/readyz=503`，即使其他租户的选中后端仍可处理请求。后续有真实部署和流量数据后再决定是否引入租户局部 readiness，当前不得擅自放宽。
10. **容量参数仍未标定**：RunnerCache 容量 32、空闲 TTL 30 分钟及各超时仍是早期保守值，没有真实租户规模、Runner 内存占用或负载指标支持。
11. **部署与观测仍缺失**：尚无本阶段 Compose/Linux 启停验收、PostgreSQL 平台仓储、端到端 OpenTelemetry、审计、租户指标、Web UI 或生产密钥管理。
12. **上游与仓库治理事项**：当前仍未配置服务仓库 `upstream`；三个官方库 PR 的维护者反馈需独立跟踪，但不应混入 Phase 2 代码或阻塞下一阶段规格。

## 十、下一窗口第一步

1. 先完整阅读本摘要，重点看“九-D”和“十-A”；再读 `docs/stage2-multi-tenant.md`、Phase 2 Plan 和逻辑 Plan，最后核对 `codex/phase2-multi-tenant-runner` 的实时 Git 状态与差异。
2. Phase 2 已完成审阅、选择性提交和推送；后续新阶段必须在个人 fork 创建 `feature/{your_name}` 分支，并提供该分支链接，不直接堆在 `main`。
3. 根目录有大量前序未跟踪资料，继续禁止 `git add -A`，也不要误纳入 `docs/upstream-issues.md`、`findings.md`、`progress.md`、各阶段 Plan 或本交接摘要等独立记录。
4. 真实模型目录 Smoke 已补验；Linux/Bash 启停闭环仍需在具备可用 shell 的环境单独验证，不伪造通过。
5. Phase 3 开工前先生成并审阅源码级规格，固定 Redis Streams、Inbox 幂等、租约/重试、ACK 顺序、错误语义和测试矩阵；未经规格审阅不开始编码。
6. Phase 3 只实现 Gateway/Worker 消息可靠性；双 Worker 与共享状态属于 Phase 4，Telegram/企业微信与 Web UI 属于 Phase 5，不提前混入。
7. 三个上游问题继续独立跟踪维护者反馈，不修改本服务 Phase 2 生产代码，也不作为下一阶段开工前置条件。

## 十-A、Phase 3 开工入口

### 当前目标

Phase 3 只解决 Gateway/Worker 的消息可靠性：让入站消息进入任务流、被 Worker 可靠领取和恢复处理，并让回复进入独立出站流；采用“至少一次投递 + 幂等处理”，不宣称分布式绝对只执行一次。

### 已确定的范围

- Redis Streams 使用 `agent.tasks` 和 `agent.replies` 两条流；Worker 使用 Consumer Group 领取任务。
- 任务至少携带 `task_id`、`tenant_id`、`agent_app_id`、`config_version`、`session_id`、`message_id`、`trace_id` 和 `attempt`。
- Inbox 候选唯一键固定为 `tenant_id + channel_binding_id + platform_message_id`，用于跨租户去重。
- Inbox 状态至少包括 `processing`、`succeeded`、`failed`；任务租约超时后允许重新认领，失败按指数退避重试。
- Worker 先写入回复事件，再确认任务；回复由 Gateway 从独立回复流发送或继续重试。
- Worker 不持有 IM 凭据；真实 Telegram/企业微信 Adapter 和真实回调联调留到 Phase 5。
- Phase 3 先验证单 Worker 的任务恢复和幂等语义；双 Worker 并发认领、Session 串行化和故障接管属于 Phase 4。

### 开工前必须锁定的细节

- Gateway、Worker 的启动参数、进程角色和本地开发启动方式。
- Consumer Group 名称、消费者命名、Pending 列表处理和租约续期/重新认领 API。
- Inbox 状态的原子转换、最大尝试次数、退避上限、最终失败保存位置和人工重放边界。
- “回复已写入但任务确认失败”以及“任务已确认但回复发送失败”的补偿语义。
- 可重试错误与不可重试错误的分类，及其到 HTTP/日志/指标的映射。
- Demo 出站发送器和 Mock 模型的测试替身，确保无需真实 IM 账号也能验收可靠性。

### Phase 3 入口门槛

1. 从 `b1ae13d`（Phase 2 已推送基线）创建个人 fork 分支，按负责人规范命名为 `feature/{your_name}`；现有 Phase 2 分支不改名。
2. 先形成并审阅 Phase 3 源码级规格、接口、错误语义、兼容性和测试矩阵，再修改生产代码。
3. 保持 Phase 2 的 `/healthz`、`/readyz`、Demo Binding 可信路由和租户隔离行为不变。
4. 通过 Redis Streams 重复投递、租约恢复、失败重试、ACK 顺序、跨租户 Inbox 唯一键和服务重启恢复测试后，才可进入 Phase 4 设计。

## 十一、最近窗口记录

### 2026-08-28：Phase 2 审阅收口与真实模型双租户 Smoke

- 清理 `ChannelBinding.CredentialRef` 及两段死校验，补充严格 JSON 拒绝测试；定向和全量 test/race/build/vet 均通过。
- 后续编号统一为 Phase 3 可靠消息、Phase 4 双 Worker 与共享状态、Phase 5 Telegram/企业微信与 Web UI。
- 使用被忽略且不含秘密的 Smoke Catalog、`deepseek-v4-flash` 和临时 Redis 7 完成 InMemory/Redis 两租户真实调用；健康、就绪、非空响应、Session 隔离和 Redis 写入均通过。
- Smoke 服务、专用容器、临时日志/二进制和三个临时用户变量已清理；用户设置的模型 Key 保留，现有容器未修改。方案 C 仍暂缓，Phase 3 尚未开始。

### 2026-08-28：负责人确认个人 Fork 分支提交规范

- 负责人确认后续代码直接在提交者自己的 fork 建立分支，分支名称使用 `feature/{your_name}`，以便按分支识别提交人。
- 后续交付应提供个人 fork 中该分支的链接；如需进入上游，再按维护者要求从该分支发起 PR。
- Phase 2 已有分支 `codex/phase2-multi-tenant-runner` 和已推送历史不改名；从 Phase 3 开始按新规范创建分支。

### 2026-08-27：Phase 2 多租户内核实现与本地验收完成

- 从 `feature/phase1.5-storage-spike@fb3cfa3` 创建 `codex/phase2-multi-tenant-runner`；旧 Phase 2 本地分支和未跟踪资料未修改。
- 完成严格 JSON Catalog、凭据引用、PresetRepository、可信 Binding、统一消息、BackendProvider、RunnerRegistry、空闲 LRU 和多租户 Runtime。
- 两租户 Redis/InMemory 配置、Runner、Session、Memory 隔离，版本切换、存储冷切换、严格 readiness、故障无 fallback、恢复和 HTTP 兼容测试通过。
- 当前工具链 test/race/build/vet、Go 1.21.13 test/build 和真实 Redis 7 Phase 2/legacy Smoke 均通过；专用容器已清理，真实模型 Smoke 未运行。
- `start.sh` 已修正为执行 `serve "$@"`；由于本机没有可用 WSL/Git Bash，Bash 启停闭环未执行。真实模型目录 Smoke 也因无外部凭据未运行。
- 实现尚未提交、暂存或推送；本轮只完成交接摘要收口。全部遗留问题留到下一轮新对话按“九-D”和“十”处理。

### 2026-08-26：Phase 1.5 主线提交已推送，回到平台主线

- 用户确认三个官方库问题已经分别正式提交给上游，并明确它们与 `trpc-agent-service` 主线无关；后续仅跟踪上游 CI 和维护者反馈，不再把创建 PR 作为本服务下一步。
- `trpc-agent-service` 的 Phase 1.5 生产代码、测试和阶段文档已提交为 `fb3cfa3 Phase 1.5: adopt official Redis session/memory services`，当前分支为 `feature/phase1.5-storage-spike`，已推送并跟踪 `origin/feature/phase1.5-storage-spike`。
- 当前工作树的跟踪文件无未提交改动；交接摘要、施工计划、过程记录和其他工作资料仍为未跟踪文件，后续不得使用 `git add -A` 误纳入主线提交。
- Phase 1.5 的主线结论保持不变：生产使用官方 Redis Session/Memory Service + 平台 `RedisBackend` 薄包装；SQL 子模块不进入当前生产依赖；下一步先编写并审阅 Phase 2 详细规格，再实现多租户和 RunnerRegistry。

### 2026-08-25：完成三个上游修复并进入 PR 提交阶段

- MySQL `%w` 日志格式问题已在 `codex/fix-mysql-log-format` 修复，两处格式指令改为 `%v`；提交 `75cb280e` 已推送个人 fork，普通测试与 vet 通过。用户曾进入 PR 准备流程，但是否最终创建 PR 未确认。
- Redis Memory 容量检查已在 `codex/fix-redis-memory-limit` 改为单键 Lua 原子操作；覆盖并发新增不得超限、满额已有 ID 幂等覆盖、脚本错误和异常返回。提交 `83a84371` 已推送个人 fork；用户明确尚未创建 PR，已提供测试、push、compare URL、英文标题和 PR 正文。
- PostgreSQL Summary 时区问题已在 `codex/fix-postgres-summary-timezone` 修复；选择保留旧 schema 和 JSON 绝对时间的兼容方案，不恢复全局 UTC。真实 PostgreSQL 16 用例从修复前 `found=false` 变为通过，普通测试、race 和 vet 均通过；临时容器与测试文件已清理，本地提交为 `7adbd288`。用户审阅后确认没有问题，已提供 push 和 PR 文稿，但是否实际推送/创建 PR 未确认。
- 三个修复均直接基于官方 `main@bcd0a567`，没有互相堆叠，也没有修改 `trpc-agent-service` 生产代码。PR 标题和正文均按官方 `CONTRIBUTING.md` 使用英文；GitHub issue 编号仍未在本地资料中确认。

### 2026-08-25：三个上游修复 worktree 准备完成

- 已在官方 `main@bcd0a5679624b85810dd6e047eb63ec8867a05f4` 复核三个主要问题仍存在，并在 `docs/upstream-issues.md` 整理 issue 标题、复现、影响、期望行为和建议 PR 边界；issue 是否已在 GitHub 实际创建及编号仍需下一窗口核对。
- 已确定三个问题对应三个独立子模块和三个独立 PR：`session/mysql`、`memory/redis`、`session/postgres`；执行顺序为 MySQL -> Redis -> PostgreSQL。
- 已创建并核验三个干净 worktree：`trpc-agent-go-fix-mysql`、`trpc-agent-go-fix-redis-memory`、`trpc-agent-go-fix-postgres`，分支分别为 `codex/fix-mysql-log-format`、`codex/fix-redis-memory-limit`、`codex/fix-postgres-summary-timezone`。
- 已确认官方远端只用于 fetch、个人 fork 用于 push；三个分支当前跟踪 `upstream/main`，首次推送时再建立对应 `origin` 上游。协调工作区中的用户文档改动保持原状，不作为修复基线。
- 已记录本机基线：MySQL 默认 test/vet 因两处 `%w` 失败而 `-vet=off` 通过；Redis 与 PostgreSQL 的 test/race/vet 通过。正式 PR 仍需补充目标回归测试、Go 1.21、仓库工具链和真实 Redis/PostgreSQL 验证。
- 本轮没有修改三个子模块的代码，没有提交、推送或创建 PR。下一窗口直接从 MySQL worktree 开始，但须先核对 issue 编号和官方分支是否已有新提交。

### 2026-08-25：Phase 1.5 已关闭并接入官方 Redis

- 在 `.stage15-spike/` 隔离消费者中固定并验证根模块 `trpc-agent-go v1.11.2`、Redis 子模块 `session/redis`、`memory/redis`、`storage/redis v1.11.0`，以及 MySQL/PostgreSQL 发布模块；使用 `GOWORK=off`、无本地源码 `replace`，记录完整模块图和传递依赖。
- Redis miniredis 与独立 Docker Redis 7 契约均通过：Session/Memory CRUD、State、Event 顺序/过滤、Summary、TTL、前缀隔离、跨实例可见性、24 路并发写、context 取消、停止/恢复和 Close 生命周期均有证据。生产故障演示保持 `/healthz=200`、`/readyz=503`、消息 `503`，Redis 恢复后无需重启即可恢复。
- 唯一 A/B/C 结论为 **B：官方 Redis Service + 平台 `RedisBackend` 生命周期薄包装**。生产根模块仍为 `v1.11.2`，Redis 子模块为 `v1.11.0`，`go-redis` 为 `v9.11.0`；旧快照适配器、`ErrUnsupported`、fallback、双读和迁移已删除/禁止。
- MySQL 完成静态/sqlmock 核验；PostgreSQL 16 真实最小契约通过，并保留官方 Summary 时区限制证据。SQL 子模块不进入生产 `go.mod`。
- 当前工具链下生产全量 `test`、`race`、`build`、`vet`，以及真实 Redis/PostgreSQL、Mock 模型端到端均完成；Go 1.21.13 下生产与隔离消费者 test/build 通过。真实 DeepSeek smoke 仍可按环境变量复验，但本阶段未重新运行，外部模型故障不作为存储选型阻塞。
- 阶段 1.5 已关闭；由于发现官方库存在可复现的小缺陷，下一窗口先准备并审阅官方反馈，暂不生成 Phase 2 详细计划或实现 Phase 2。

### 2026-08-25：本轮对话收尾与官方反馈安排

- 用户确认 Docker 当前已启动；阶段 1.5 专用 Redis/PostgreSQL 容器此前已完成验收并清理，后续可按官方问题单需要重新启动隔离容器。
- 本轮已明确：PostgreSQL Summary 时区缺陷、MySQL Session `%w` 日志格式/vet 缺陷、Redis Memory 有限容量检查非原子，均应作为下一轮对官方的首要反馈项；它们不改变当前 B 路径结论。
- 当前生产规避措施保持不变：SQL 子模块不进入生产依赖；Redis Memory 使用无限容量；不引入隐式 fallback、双读、运行时开关或对官方 CRUD 的二次包装。
- 当前工作区仍有 Phase 1.5 代码、测试和文档未提交/未暂存；未配置 `upstream`。本轮只更新交接记录，不执行提交、推送或上游 issue 创建。

### 2026-08-25：纠正多模块适配器结论并准备 Phase 1.5

- 确认 DeepSeek 先前检查只覆盖根模块 `trpc-agent-go@v1.11.2`，未覆盖独立子模块，因此“官方没有 Redis/SQL 适配器”的结论不完整。
- 本地 `trpc-agent-go-multi-tenant-workspace` 是 tRPC-Agent-Go 多模块源码树，实际包含 Session Redis/MySQL/PostgreSQL/SQLite/MongoDB、Memory Redis/MySQL/PostgreSQL/SQLite、Knowledge Qdrant/Milvus、Artifact S3 和相应 `storage/*` 子模块。
- Go Module Proxy 与实际下载确认这些子模块存在远程正式版本；Redis 主线多数为 `v1.11.0`，MySQL Session 已有 `v1.11.2`，Qdrant/Milvus/S3 等也有独立发布版本。
- 修正事实口径：根模块目录不包含独立子模块，不等于远程没有适配器；远程有发布版本，也不等于当前服务已经兼容或接入。
- 决定在 Phase 2 前新增 Phase 1.5，验证官方 Redis/SQL 子模块，决定替换、薄包装还是保留 Phase 1 自研 Redis。
- 按 README 验收标准第 7 条建立“框架复用 vs 平台新增”核对矩阵；后续阶段必须引用包/子模块和证据来源，避免重复自研官方已有能力。
- 该记录发生在 Phase 1.5 执行前：当时决定新增阶段并准备 Spike。随后已完成验证和 B 路径生产接入，当前状态以上方“Phase 1.5 已关闭并接入官方 Redis”记录为准。

### 2026-08-24：Phase 1 最小闭环实现并完成本地验收

- 锁定 tRPC-Agent-Go `v1.11.2`、go-redis `v9.7.0` 与 OpenAI Go `v1.12.0`，Go module 保持 `go 1.21`。
- 实现 `trpc-service serve [-addr :8080]`、`/healthz`、`/readyz` 和 `/api/v1/demo/messages`。
- 实现配置 fail closed、Redis URI 规范化、HMAC 身份派生、RunnerCache、executor 和 Redis Session/Memory 最小适配。
- 真实 DeepSeek 两种 Base URL、真实 Redis Session/Memory 跨 runtime、Mock HTTP 端到端、4xx/5xx/空响应/超时和 Redis 故障均已验证。
- `go test ./...`、race、build、vet 和 Go 1.21.13 全量测试通过；Phase 1 详细结果见 `docs/stage1-spike.md`。
- 当时只检查了根模块 `v1.11.2` 的下载目录，因未检查独立子模块而误判“框架没有 Redis Session/Memory 包”；该结论已于 2026-08-25 纠正。Phase 1 快照适配仍只服务最小闭环，跨进程原子写等能力仍是后续阻塞项。
- 宿主 Docker/Redis 正常；Codex 默认沙箱的 named pipe 访问限制已与宿主状态明确区分。
- 当时尚未配置 upstream、创建分支、暂存、提交或推送；随后 Phase 1 已提交并推送，见 2026-08-25 记录。

### 2026-08-24：Phase 1 审阅通过与 Docker Redis 真实联通确认

- 外部审阅结论：Phase 1 已完成，可进入 Phase 2；完成范围明确为单进程最小闭环，不包含真实 IM、双 Worker、SQL、队列和完整生产 Redis 后端。
- 真实 Redis 联通再次确认：`miniredis` 只用于自动化单元测试；真实服务通过 `REDIS_URL=localhost:6379` 连接 Docker 中的 `trpc-redis`，`/readyz` 通过真实 PING，消息处理产生的 Session/Memory 数据真实写入 Redis。
- 真实演示使用 `deepseek-v4-flash`，返回固定验证文本；演示 key 使用 `phase1-live:*`，验证后已清理，未把 API Key 写入日志、文档或 Git。
- Docker 访问约束已固化：默认沙箱不变，需要 Docker 时按单命令申请临时提权，只允许访问 Docker；不修改全局 Codex 配置，不开启长期 `danger-full-access`。
- 当时准备进入 Phase 2；2026-08-25 因独立子模块事实纠正，已在 Phase 2 前插入 Phase 1.5。

### 2026-08-24：完成阶段 0 调研并形成审阅材料

- 只读核对当前服务仓库：仍是可编译骨架，没有 Agent/Runner、IM、Gateway/Worker、共享存储或多租户实现；`go test ./...` 和 `go build ./...` 通过，但所有包均无测试文件。
- 确认 tRPC-Agent-Go 的 GitHub 地址和 Go module path 不同；Go 必须引用 `trpc.group/trpc-go/trpc-agent-go`。Go module proxy 与 GitHub tag 共同确认当前最新稳定版为 `v1.11.2`，Go 1.21。
- 在独立临时模块编译验证 `NewRunnerWithAgentFactory`、Session/Memory Service 注入和 `Runner.Close`，未修改服务仓库 `go.mod`。
- 源码确认 Session/Memory key 都包含 AppName，且两个 Service 都是包含状态、事件、异步任务和 Close 的宽接口。旧方案的确定负担是 Wrapper 覆盖和生命周期复杂，不把 AppName 漏传/错传作为既定缺陷。
- 阶段 0 推荐新候选方案：每个 Worker 按 `tenant_id + agent_app_id + config_version` 有界缓存 Runner；两个 Worker 的 Runner 实例独立，配置和共享 Session/Memory 后端一致。该推荐仍需用户审阅，未实现。
- 发现群聊作用域问题：框架 Session key 同时含 UserID 与 SessionID。首版建议群聊使用合成群主体作为 runner_user_id、真实 actor 单独用于权限审计，因此群聊 Memory 也是群级；共享群 Session + 个人 Memory 需要后续专门设计。
- Telegram 推荐首选 `go-telegram/bot v1.23.0`、备选 `telegram-bot-api/v5 v5.5.1`；企业微信主要 SDK 候选为 `silenceper/wechat/v2 v2.1.14`，但必须先确定内部应用、客服、群机器人等具体协议类型。
- 已形成五份审阅文档：`docs/stage0-baseline.md`、`docs/stage0-im-sdk-matrix.md`、`docs/stage0-runner-options.md`、`docs/stage0-architecture.md`、`docs/stage0-acceptance.md`。
- 已定义自有统一消息、可信 ChannelBinding、Inbox 候选唯一键、Session ID、Gateway/Worker/Adapter/Runner 边界和 fail-closed 矩阵。OpenClaw 仍只作设计参考，无直接依赖。
- 当前网络足以访问 Go module proxy、GitHub 页面和远端 tag；GitHub REST/搜索曾遇连接关闭和限流，但不阻塞本阶段结论。
- 阶段 0 临时编译模块和 Go 缓存已在最终验证后删除，服务仓库未保留 `.stage0-compile`。
- 本阶段没有执行 `go get`、没有引入 IM SDK、没有修改生产功能、没有创建分支、提交或推送；当时的审阅门槛已在本日后续记录中完成。

### 2026-08-24：阶段 0 五项决策正式确认

- Runner 方案已确认：每个 Worker 按 `tenant_id + agent_app_id + config_version` 使用有界本地 Runner 缓存；两个 Worker 不共享 Runner 对象，共享 Session/Memory 后端。
- Telegram 已确认首选 `github.com/go-telegram/bot v1.23.0`，`telegram-bot-api/v5 v5.5.1` 为备选。
- 企业微信已确认首版按内部自建应用验证，`github.com/silenceper/wechat/v2 v2.1.14` 先做协议匹配 Spike；真实账号联调后置，Mock 可用于自动化测试。
- 群聊已确认使用群主体作为 `runner_user_id`，Session/Memory 为群级作用域；真实 `actor_user_id` 仅用于权限和审计，首版不承诺个人 Memory。
- 最小真实交付和 Web UI 边界已确认：真实 Agent、双 IM、Gateway/双 Worker、共享 Redis、基础治理/观测必须编码；生产扩展以设计文档为主，Web UI 只做聊天演示和模拟绑定。
- 阶段 0 已正式关闭。阶段 1 开工前仍需完成依赖与 Runner Spike、生成详细实施规格；本轮没有引入 SDK、没有修改 `go.mod` 或功能代码。

### 2026-08-24：准备进入阶段 1

- 已更新本交接摘要，将当前状态从“阶段 0 等待审阅”改为“阶段 0 已关闭、阶段 1 入口规格与 Spike 准备中”。
- 阶段 1 的第一步不是直接写生产功能，而是完成 RunnerCache、Telegram/企业微信 SDK 和 tRPC-Agent-Go 版本的临时验证，再锁定 `go.mod`。
- 已明确当前问题：真实模型 endpoint/凭据尚未提供；Redis/PostgreSQL 本地运行方式尚未核验；`upstream`、开发分支和 Git 安全目录尚未配置。
- 上述问题中，模型凭据只阻塞真实模型演示，Redis/PostgreSQL 运行方式阻塞多节点验收，Git/upstream 配置阻塞后续正式贡献；均不阻塞阶段 1 规格编写。
- 本轮只更新交接摘要，没有修改功能代码、没有引入依赖、没有运行阶段 1 Spike、没有创建分支或提交。

### 2026-08-23：纠正 OpenClaw 复用边界并暂缓 Runner 方案定案

- 确认不直接复用或包装 OpenClaw API。`openclaw` 下的 Telegram 是可运行成品示例的一部分，不把它当作供 `trpc-agent-service` 直接导入的 Telegram SDK。
- OpenClaw 的 Telegram 分层、插件注册、统一消息、会话、附件和重试设计可以作为参考，但是否采用类似插件体系、接口如何组织，仍以当前项目实际要求和后续实现调查为准。
- 按照负责人的思路，两种 IM 优先使用 GitHub 上已有的成熟 SDK，再由项目编写统一 Adapter；具体 SDK、最终 IM 组合、企业微信接入类型和真实联调方式留待下一轮讨论。
- 旧 Runner 方案可概括为“所有请求共用一个 Runner，运行过程中由路由 Wrapper 选择对应数据后端”；新候选方案可概括为“执行前先确定 Agent App，再选择该 Agent App 对应的 Runner”。
- 纠正此前解释：不能把“所有地方都必须正确携带 AppName，容易漏传或错传”直接认定为旧方案的既定问题；旧方案当前可以确定的主要代价是路由 Wrapper 需要覆盖完整能力和生命周期，复杂度与维护成本较高。
- 用户对“每个 Worker 按 Agent App 缓存 Runner”的新候选方案仍有未理解之处，下一轮先继续解释和比较，不将其视为已确认的最终方案。
- 本轮只更新交接摘要，没有修改功能代码、没有安装依赖、没有运行测试、没有执行 Git 分支、提交或推送操作。

### 2026-08-23：确认双 IM、Web UI、多节点和实施范围

- 负责人明确“两种 IM”必须在代码中实现，Web UI 不能替代两种 IM；Web UI 作为类似常见大模型聊天页的本地验证和演示入口。
- 当前实施口径确定为“真实 SDK/协议实现 + 可追溯 Mock 数据验证”，为以后填写真实配置和扩展新 IM 保留清晰模块边界。
- IM 组合暂定 Telegram + 企业微信；两者统一为平台消息格式。该记录中“Telegram/OpenClaw 公开 API 复用仍需 Spike”的状态已被本轮结论替代：不直接复用 OpenClaw API，改为调研 GitHub 成熟 IM SDK；企业微信具体接入类型仍待定。
- 多节点验收按个人项目的合理边界执行：同一台机器启动两个 Worker 进程或容器，不要求两台物理机器。
- 再次确认 Redis 负责即时、竞争和带 TTL 的共享状态，SQL 负责长期配置、绑定和审计；Redis 是代码最低保证，SQL 为优先项但不阻塞早期闭环。
- Web UI 只做聊天主界面和简单租户/Agent 选择，不做完整管理后台；租户选择仍须经过服务端可信绑定，不能直接信任浏览器提交的 `tenant_id`。
- 明确容量估算、完整迁移、灰度发布、生产级 Kubernetes 等以设计文档为主；核心 Agent、双 IM、多租户、Redis、双 Worker、基础可靠性和观测必须形成可运行代码。
- 新增工程指导备忘：保持模块化，持续小步重构，及时拆分超长文件和函数，核心逻辑随实现补充单元测试。
- 本轮没有创建新的 service 施工版；下一步仍是基于最新仓库状态编写并评审新施工计划，然后再开始阶段实现。

### 2026-08-21：切换到独立服务仓库并建立新交接入口

- 确认实际开发仓库改为 `trpc-agent-service`，旧 `trpc-agent-go` 工作区只保留为研究参考。
- 只读核验新仓库仍是项目骨架，尚未引入 tRPC-Agent-Go，也没有实际服务链路。
- 确认使用开发时最新稳定 tRPC-Agent-Go 并锁定版本、Gateway/Worker 同仓库双角色、Redis/SQL 分工，以及最终向原始仓库提交 PR；该记录中“按 Agent App 缓存 Runner”已在本轮降级为待继续解释和确认的候选方案。
- 澄清 Go 1.24 的来源：旧仓库根模块仍为 Go 1.21，`test` 模块为 Go 1.24.4，OpenClaw 模块为 Go 1.24.1。
- 确认平台层建立自己的 Channel 契约；该记录中“是否直接依赖 OpenClaw 仍需 Spike”的状态已被本轮结论替代：不直接依赖 OpenClaw API。
- 记录负责人允许 Mock、可提供企业微信环境和要求前端模拟展示的反馈，并明确其中尚待确认的验收含义。
- 本轮只创建交接摘要，没有开始功能开发或 Git 操作。
